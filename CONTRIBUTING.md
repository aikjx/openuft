# 贡献指南 · Contributing to openuft

> 「算法联盟 · 全维统一场论研究」是开放的科学工程，欢迎一切形式的贡献——理论、证明、验证代码、可视化、应用案例、文档润色。

## 一、四方法主轴（4 Pillars of Contribution）

所有贡献请选择以下 4 个主轴之一：

| 主轴 | 路径前缀 | 核心问题 | 适合谁 |
|---|---|---|---|
| **D 求导** | `10_D_求导_derivation/` | 如何从公理/作用量严格导出方程？ | 理论物理学家、变分法研究者 |
| **P 证明** | `20_P_证明_proof/` | 如何把直觉/几何关系变成机器可验证的严格证明？ | 数学家、sympy/mpmath 专家 |
| **V 验证** | `30_V_验证_verification/` | 数值结果是否对得上 PDG / LIGO / Planck 等实验？ | 计算物理学家、实验物理学家 |
| **A 精算** | `40_A_精算_audit/` | 误差/风险/诚实分层是否经得起第三方审查？ | 统计学家、风险精算师、科学哲学家 |

**物理领域**（电磁、弱力、强力、引力、电弱统一、大统一、宇宙学、量子引力）放在 `50_physics_domains/`。<br>
**工业应用**（磁约束、加速器、磁场质量、模拟验证基准）放在 `60_application/`。<br>
**未来扩展**（大统一完成、弦论/LQG 统一、暗物质等）放在 `99_inbox_future/`。

## 二、贡献流程（5 步）

1. **Fork** + 创建特性分支：`git checkout -b feat/your-contribution`
2. **选定主轴**：在新分支下创建相应子目录，命名遵循 `P?_新定理_代号_R?` 或 `D?_新版块` 等
3. **撰写文件**：至少包含证明/推导/验证的完整过程，所有数值结论给出**精度估计**
4. **自验证**：使用本项目的 `70_source_code/triad_uft/` 工具链验证你的贡献（如适用）
5. **Pull Request**：标题格式 `[主轴] 简短描述`，例如 `[P] 新增 R12 弯曲时空三重奏证明`；正文填写 `PULL_REQUEST_TEMPLATE.md` 中的所有勾选项

## 三、严格度要求（不可妥协）

### 数学严格性（要求 ★★★★☆ 以上）
- 所有"定理"必须有机器可验证的证明（sympy / mpmath / Lean 4 / Coq 等）
- 数值结论必须给出**精度下限**，例如「mpmath 50 位相对差 ≤ 1.17e-18」
- 不得使用"显然"、"可以验证"、"容易看出"等无证据短语

### 物理诚实性（要求 ★★★★☆ 以上）
- 必须区分「已严格证明 L5」「已严格推导 L4」「定量符合 L3」「定性对应 L2」「数值可疑 L1」「未触及 L0」六个分层
- 不得包装为「万有理论」「量子引力解决」「暗物质发现」等
- 必须把已知偏差写明（如 `M_W/M_Z` 偏差 < 0.4% 而非 ≈0）

### 工业可用性（要求 ★★★☆☆ 以上）
- Python 代码必须能 `pip install -e .` 安装
- 关键函数必须有单元测试（测试矩阵 ≥ 72 项）
- 文档必须包含**最小可复现示例**（Minimal Reproducible Example, MRE）

## 四、不接受的贡献（反例清单）

| 不接受 | 原因 |
|---|---|
| 仅做哲学/思辨而无论文产出 | 科学需要可证伪可验证 |
| 宣称已统一四种基本力但无 R9 级证明 | 必须经过 sympy/mpmath 复算 |
| 数值结论无精度估计 | 违反"诚实分层"原则 |
| 把开放问题标记为"已解决" | 违反 A4 诚实声明 |
| 把 README 改成 AI 美化样式而非内容实质提升 | 形式掩盖内容违反精算原则 |
| 未经许可修改 LICENSE | 法律红线 |

## 五、代码风格

- Python：PEP 8 + Black + isort，类型注解必备，docstring 用 Google 风格
- Markdown：标题 ≤ 4 层，加粗关键词使用 `**` 而非 `__`，公式用 LaTeX（行内 `$...$`，块级 `$$...$$`）
- 中文文档：标点使用全角（，。；：「」），英文与中文之间留一空格

## 六、版本与 CHANGELOG

任何贡献必须在 `CHANGELOG.md` 顶层「未发布」区域添加一行，遵循 Keep a Changelog 规范：

```
## [Unreleased]
### Added
- 新增 P11 弯曲时空三重奏 R12 严格证明（你 · 2026-XX-XX）
### Changed
### Fixed
### Deprecated
### Removed
```

---

**共同守则**：开放、严谨、诚实、可复算。「不知道」比「错」好 1000 倍，「我错了」比「我对了」更需要勇气。

— AI 科技星
