# 突破：规格曲线钉死不可行性、自指近消去恒等与自然输入普查（定理 R）

**引擎**：`源码/突破_规格曲线钉死与自指近消去.py`（可复跑，sympy + mpmath dps=50）

> 承接 `突破_算子谱与量子化规格.py`（定理 F）末句提出的真问题：
> **「什么把 (A,c) 钉在规格曲线上？」** 本轮正面攻之。

## 一、勘误：规格方程的精确形式

由 $u^*=2Q/P$、$\alpha=1/\sqrt{u^*-1}$ 严格推出精确规格方程：

$$\boxed{\ \alpha^2=\frac{P}{2Q-P}=\frac{A+c/2}{\tfrac12+\tfrac{c}{2}-A}\ }$$

既有文档写的 $\alpha^2=|A+c/2|/|c+1/2|$ **不是精确解**（仅在近消去极限 $A\to-c/2$ 与之同阶，差 $O(\alpha^2)$）：

| A | c | α² 精确 | α² 旧式 | 相对差 |
| --- | --- | --- | --- | --- |
| 0.00 | -2.0 | 2.0 | 0.6666666666666666 | 6.667e-01 |
| 0.00 | -5.0 | 1.25 | 0.5555555555555556 | 5.556e-01 |
| 0.25 | -1.0 | 1.0 | 0.5 | 5.000e-01 |
| 0.40 | -1.0 | 0.25 | 0.2 | 2.000e-01 |
| 0.60 | -2.0 | 0.36363636363636365 | 0.26666666666666666 | 2.667e-01 |

> 近消去极限处（$A=0.499973376$，$c=-1$）旧式与精确式同阶（都 $\approx\alpha^2$），故此前未被发现。

## 二、定理 R1：自指近消去恒等（精确代数恒等）

规格方程两边同乘 $(2Q-P)$ 得 $P=2Q\alpha^2/(1+\alpha^2)$，即

$$\boxed{\ \left|\frac{P}{Q}\right|=\frac{2\alpha^2}{1+\alpha^2}\ }\qquad(\text{与 }c\text{ 无关})$$

- 要求机制实现的**近消去比例** $|P/Q|$ = **0.000106497**；
- 在 $c=-1$ 处把 α 钉到 $\alpha_{\rm obs}$ 所需的 $A$ 相对精度 = **5.324852e-05**，
  而 $\alpha^2/(1+\alpha^2)$ = **5.324852e-05**，恒等残差 = **9.29e-53**。

> **含义（循环性）**："需要供给的精度" 恒等于 "待预言的量"（$O(\alpha^2)$ 级）。
> 要钉住 α，机制必须**已经知道** α。这不是数值巧合，是代数恒等。

## 三、定理 R2(a)：离散拓扑输入 —— 可证伪否定

枚举范围：$c\in\{-1,-3/2,-2,-5/2,-3,-4,-5\}$、$\varphi\in\{0,1/2,1/3,2/3,1/4,3/4,1/5\}$、$N\le4000$。

| 量 | 值 |
| --- | --- |
| 距离最近解 $(c,\varphi,n,N)$ | (-4/1, 3/4, 2853, 2018) |
| 预言 $1/\alpha$ | **137.0379** |
| 相对偏差 | **1.369e-05** |
| 观测精度 1.6e-5 | 偏差为其 **0.9 倍** |

> ⇒ 全枚举内 $1/\alpha_{\rm obs}=137.036$ **不达**；离散路被观测**可证伪否定**
> （更大 $N$ 的逼近属丢番图逼近，非预言）。

## 四、定理 R2(b)：近消去归约（无标度壁垒适用）

规格方程只依赖比值 $P/Q$（$c=-1$ 时 $Q=-1/4$ 固定 ⇒ 只需 $P$ 的绝对值）。
$P\to0^-$ 的近消去等价于**小正质量平方 / 近临界**精细调节（与层级问题同型）。
定理 E（齐次结构定不了绝对标度）+ 定理 N（纯相位 $Z$ ⇒ $\beta\equiv0$，无 RG 环）
⇒ **框架内无生成残差 $|P/Q|=2\alpha^2/(1+\alpha^2)$ 的机制**。

## 五、突破候选普查（无调参自然组合）

枚举 $c$ 分母 $\le5$、$\varphi$ 分母 $\le10$、$N\le6000$ 的全部组合。

| 量 | 值 |
| --- | --- |
| 命中观测精度 $1.6\times10^{-5}$ 的组合数 | **13** |
| 最佳整体 $1/\alpha$ | 137.0357（$c$=-5/1, $\varphi$=1/7, $n$=7619, $N$=4819）偏差 2.08e-06 |
| 小绕数最佳（$N\le300$） | 137.1528（偏差 8.52e-04，$N$=234） |

> ⇒ **无调参自然命中**；所有接近解都在大 $N$（丢番图逼近）或需把 $(A,c)$ 调到规格曲线上。

## 六、定理 R 与路线总账

| 路线 | 结论 | 封死者 |
| --- | --- | --- |
| 量纲代数（锚集零空间） | 不可行 | 定理 C |
| 几何作用量（旋钮零空间） | 不可行（无内点极小） | 定理 H |
| 算子谱+量子化（可固定 α） | 可行但需近消去 | 定理 F |
| 规格曲线钉死（本轮） | 不可由本公设闭合（离散被证伪/连续为输入） | 定理 R |

**诚实边界（不美化）**：
- 定理 R1 是精确代数恒等（|P/Q|=2α²/(1+α²)），非数值巧合。
- 定理 R2(a) 的可证伪否定限于已枚举的 (c,φ,N) 网格与 N≤6000；更大 N 属丢番图逼近，不作预言。
- 定理 R2(b) 引用的定理 E/N 出自 openuft 既有体系（本书第十二/十四编），本轮只做对接，不重证。
- 本引擎不解锁 UFT-3、不宣称推出 α；它把"什么钉住 (A,c)"判为不可由本公设闭合。

> **下一个真问题**：剩余唯一出口：一条**既非齐次、又带独立无量纲锚**的耦合约束。在本框架内已穷尽（定理 E/F/N + 本轮 R）；须引入强于世界线几何的新动力学公设。

> **诚实立场**：本轮**没有**实现统一场论、**没有**解锁 UFT-3（联盟层仍 2/6）。
> 它把"什么钉住 (A,c)"从**未试出**升级为**已证不可由本公设闭合**：
> 离散输入被观测证伪，连续输入落到定理 F 输入壁垒，近消去本身是 $2\alpha^2$ 级的自指循环。