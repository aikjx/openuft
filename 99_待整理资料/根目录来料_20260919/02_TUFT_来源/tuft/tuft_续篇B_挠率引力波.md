# TUFT 续篇·选项 B：挠率诱导引力波（定量 + 诚实边界）

> 承 `tuft_全维分析续篇_自旋统计泡利手性.md` §九 之建议，选 B。
> 数值实现：`tuft_引力波_挠率扰动.py` → `tuft_引力波_挠率扰动_report.txt`（PASS=4 / FAIL=0 / INFO=8）。

---

## 一、最关键结论（先讲诚实边界）

TUFT 的引力扇区（R1 已验证：`g=(c²/2)∇lnβ1`，场方程 `□u=−(8πG/c²)ρexp(−u)`）在弱场下满足**与广义相对论完全相同的无质量波动方程**。因此：

- **传播**：波速 = c、无色散、1/r 衰减、张量偏振 —— 与 GR **完全一致，无 TUFT 独有增量**。
- **TUFT 唯一可区分预言**：一个**标量"呼吸"引力波模式** —— 由挠率/拓扑结的振荡直接驱动 `β1` 的径向脉动（`δ(lnβ1)=δu` 即度规径向应变 `h_radial=δu`）。GR 只有张量 TT 模，没有标量呼吸模。

> 这是诚实且可证伪的：TUFT 在引力波*传播*上不做任何新预言，只在*源/模态*上多一个标量通道。

---

## 二、符号证明（§1，PASS）

线性化 TUFT 场方程：

```
(1/c²) ∂²δu/∂t² − ∇²δu = −(8πG/c²) δρ
```

与 GR 弱场牛顿势波动方程 `(1/c²)∂²Φ−∇²Φ = −4πGδρ`（取 `Φ=(c²/2)h_{00}`）同构（差常数因子 2）。算子无质量项 ⇒ 速度 c，无色散。

---

## 三、有限差分数值验证（§2，PASS）

球对称有限差分（无量纲 `L0=GM/c², T0=L0/c`，海绵层吸收外边界反射）：

| 检验 | 结果 | 理论 |
|---|---|---|
| 波前传播速度 | 斜率 = **0.984** | 1.0 |
| 辐射衰减 `δu(r)·r = 常数` | 比值 = **1.056** | 1.0 |
| 标量模峰值应变 h_toy（r=700 L0，源 A=1e-4） | **7.5e-2** | — |

→ TUFT 几何场的确以光速发射并在辐射区按 1/r 衰减，与 GR 张量模行为同构；标量呼吸模幅度由源强度 A 与自由耦合 ξ 决定。

---

## 四、对标 LISA（§3，INFO 标度估计）

物理标度（δu 为无量纲应变比，单位无关）：

```
h(r_phys) = h_toy · (ξ/A_src) · (r_det_toy / r_phys_in_L0)
r_phys_in_L0 = r_phys / (G·M_src/c²)
ξ = 自由挠率-度规耦合（尚未由 TUFT 作用量变分固定）
```

代表源：星系中心超大质量拓扑结，`M=1e6 M_⊙`，`d=100 Mpc`，LISA mHz 带：

| 量 | 值 |
|---|---|
| ξ = 1 时标量模应变（量级） | **h ~ 2.5e-10** |
| LISA 可检测（h~1e-21）所需 ξ 阈值 | **ξ_thr ~ 4e-12** |

### 可证伪约束（诚实标注）

- 若 `ξ ~ O(1)`，TUFT 标量呼吸模应变（~1e-10）**远超**现有 GW/PTA 上限 ⇒ 要么 `ξ ≪ 1`，要么该标量模**不与检验质量耦合**（仅挠率自作用）。这是 TUFT 的硬性边界，而非已观测信号。
- TUFT 张量模预言 = GR，无增量；唯一可区分预言即标量呼吸模。LISA 对纯标量偏振灵敏度弱于张量，更干净检验在**脉冲星计时阵列（nHz）**。
- 下一步：将 ξ 在 TUFT 总作用量中具体化为"挠率-度规耦合项"，方可给出定量 `h(f)` 曲线；当前只能量级标度阈值。

---

## 五、与 R1 地基的衔接（诚实）

R1 已数值验证：牛顿极限（误差<5e-2）、非线性自屏蔽 `1−M_eff∝GM/(c²a)`、扰动以 c 传播（Huygens）。本章在其上叠加**挠率振荡源**，复用同一波动算子，逻辑链条闭合且自洽。

> **红线**：数学自洽 ≠ 物理实验证实。ξ 为自由参数；以上"预言"均为显式 ξ 假设下的标度估计，不构成对 TUFT 物理真实性的主张。

---

## 六、修订注记（2026-09-15 排查修复）

本节由后续排查（`tuft_B_排查_线性化质量项.py`，P5/F5/B1/I7）追加，修正本文件两处结论：

1. **§2 "波动方程 ≡ GR、无色散" 不成立**：
   - 本文件的线性化 `□δu = −(8πG/c²)δρ` 相当于把 `exp(−u)` 当 1，**丢掉了背景 ρ₀ 的贡献**；
   - 严格线性化（保留 `exp(−u)`）给出 **有质量 Klein-Gordon**：`□δu + m²·δu = ...`，
     `m² = (8πG/c²)·ρ₀·e^{−u₀}`（符号与移项约定相关，但 **ρ₀ ≠ 0 时非零**）；
   - 后果：**存在色散**，传播模与 GR **有增量**。数值：中子星密度下 `m_eff = 1.37e−4 /m`（尺度 **7.3 km**）。
2. **§5 "非线性自屏蔽" 应改注**：
   - 本文件把它作为"R1 已验证"的**正面**事实引用；
   - 但后续判决（`tuft_B_自屏蔽_数值求解`）证明：黑洞 `ξ = 1/2` 时 `M_eff/M = 0.5318`
     （**偏离 46.82%**），**远超 LIGO 10% 约束 ⇒ 被排除**；
   - 应改为"**已被观测排除的缺陷**"，而非成果。
3. **同源**：静态（有效质量不足）与动力学（KG 质量项/色散）**都来自同一个 `exp(−u)` 因子**——
   本文件把 `exp(−u)` 线性化为 1，因而**同时漏掉**两者。
4. **§3 的标量呼吸模**（`ξ_thr ~ 4e-12`）**仍然有效**（是 TUFT 的独立可证伪预言），
   但需与上述两处缺陷一并理解；`ξ` 仍未由作用量变分固定。

> 修复动作：本注记已永久保留在本文件末尾（红线要求不删改不利结果，只追加注记）。

---

*配套：`tuft_引力波_挠率扰动.py` / `tuft_引力波_挠率扰动_report.txt`；修订依据 `tuft_B_排查_线性化_report.txt`。*
