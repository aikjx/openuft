# 判别式 V 的求导证明与漏洞分析（可复跑产物）

> 本文件由 `源码/判别式V_求导证明.py` 生成，**请勿手工编辑**。

## 定义

```
V(h, f, a) = (h - f - a) / h = 1 - (f + a) / h

h = n_hit  f = n_free  a = n_anchor  s = f + a
```

## 检查项

| # | 检查 | 结论 | 细节 |
| --- | --- | --- | --- |
| — | P1 偏导数闭式 ∂V/∂h=(f+a)/h², ∂V/∂f=∂V/∂a=−1/h | ✅ | sympy 求得 ∂V/∂h=(a + f)/h**2，∂V/∂f=-1/h，∂V/∂a=-1/h；与闭式逐一相等（残差 0） |
| — | P1b 单调方向：∂V/∂h ≥ 0（多命中靶只增不减），∂V/∂f = ∂V/∂a = −1/h < 0 | ✅ | 命中靶数增加 → V 上升；自由参数或测量锚增加 → V 下降。两者符号相反且严格 |
| — | P2 上确界 sup V = 1，在 s = f + a = 0 处取到 | ✅ | V = 1 − s/h；sympy 代入 s=0 得 V = 1（恒等 1）；h=1..199 数值网格实测 max = 1.000000000000 |
| — | P2b 下无界：inf V = −∞（输入数相对命中数线性增长时） | ✅ | 令 s = t·h，则 V = 1 − t，随 t→∞ 趋于 -oo。故 V 无下界，判 C 的体系之间没有可比性（只能与外部一道'+1'的成本口径比） |
| — | P3 不变性：V 只依赖 s = f + a，无法区分「自由参数」与「测量锚」 | ✅ | 代入 (f,a)→(f+1,a−1) 后 V 变化量 = 0。**这是本判别式的显式局限**：V>0 是派生的**必要条件**而非充分条件，'用什么换来的'这一维度必须由人工登记补足。这是未闭合项，不粉饰。 |
| — | P4 判定域等价于比较 h 与 s：V>0 ⟺ h>s，V=0 ⟺ h=s，V<0 ⟺ h<s | ✅ | h=1..14、s=0..14 共 210 个格点全部吻合判定域边界；不一致=[] |
| — | P5 边际递减：∂²V/∂h² = -2*s/h**3 < 0，多命中一个靶的收益随 h 增大而衰减 | ✅ | 二阶导数恒负（s>0, h>0）；s=3 时 ∂V/∂h 序列严格单调下降（无反例） |
| — | P6 判定域分布（h=1..10, s=0..10，共 110 格点） | ✅ | A=55 B=10 C=45；行内从左(s=0)到右(s=10)：<br>h=1  A B C C C C C C C C C<br>h=2  A A B C C C C C C C C<br>h=3  A A A B C C C C C C C<br>h=4  A A A A B C C C C C C<br>h=5  A A A A A B C C C C C<br>h=6  A A A A A A B C C C C<br>h=7  A A A A A A A B C C C<br>h=8  A A A A A A A A B C C<br>h=9  A A A A A A A A A B C<br>h=10 A A A A A A A A A A B |
| — | P7 与已知人工结论的对齐重核（4 例） | ✅ | α_grav(e)=G·m_e²/(ℏ·c)（M02 型）：h=1 s=4 V=-3.00 -> C（期望 C）✓；N_gen=3 由 SU(2)₂ 三扇区导出（R17 路径）：h=1 s=1 V=0.00 -> B（期望 B）✓；假想合格候选（命中 4 靶）：h=4 s=3 V=0.25 -> A（期望 A）✓；过拟合等价（3 参数命中 3 靶）：h=3 s=3 V=0.00 -> B（期望 B）✓。这是一致性检验：判别式必须能反向复现已有人工作的结论，否则它只是事后口径 |
| — | P8 **漏洞**：相关靶刷分攻击成立（朴素 V 可被人为抬高） | ✅ | 在只消耗 4 个测量锚的前提下，把同一个自由度 t=m_e/m_P 的幂次逐个登记为'靶'，V 从 −3.00 一路升到 +0.667（判定由 C 变 A）。<br>声称 h= 1：(t) V=-3.000 -> C<br>声称 h= 2：(t, t^2) V=-1.000 -> C<br>声称 h= 3：(t, t^2, t^3) V=-0.333 -> C<br>声称 h= 4：(t, t^2, t^3, t^4) V=+0.000 -> B<br>声称 h= 6：(t, t^2, t^3, t^4, t^5, t^6) V=+0.333 -> A<br>声称 h= 8：(t, t^2, t^3, t^4, t^5, t^6, t^7, t^8) V=+0.500 -> A<br>声称 h=12：(t, t^2, t^3, t^4, t^5, t^6, t^7, t^8, t^9, t^10, t^11, t^12) V=+0.667 -> A<br>⇒ 朴素 V **可被刷分**，必须修补。 |
| — | P9 修正版 V2 = (h_eff − s)/h_eff，h_eff = 函数独立的靶数（秩） | ✅ | 把同一个自由度的幂次合并计数后，V2 恒为 −3.00(C)，不再随声称数量上升。<br>声称  1 个幂次靶：朴素 V=-3.000(C) vs 有效 V2=-3.000(C)<br>声称  2 个幂次靶：朴素 V=-1.000(C) vs 有效 V2=-3.000(C)<br>声称  3 个幂次靶：朴素 V=-0.333(C) vs 有效 V2=-3.000(C)<br>声称  4 个幂次靶：朴素 V=+0.000(B) vs 有效 V2=-3.000(C)<br>声称  6 个幂次靶：朴素 V=+0.333(A) vs 有效 V2=-3.000(C)<br>声称 12 个幂次靶：朴素 V=+0.667(A) vs 有效 V2=-3.000(C)<br>⇒ V2 修补了 P8 的刷分漏洞；但它**没有**修补 P3（自由参数 vs 测量锚不可区分）——后者需人工登记粒度，这是显式登记的未闭合项。 |
| — | P9b V2 的判定域与 V 同构（仍等价于比较 h_eff 与 s） | ✅ | V2 = 1 − s/h_eff，判定边界同样是 h_eff = s；故 P4 的结论可直接迁移，两套判定不需要重新校准 |

> V / V2 是**审计工具**，不产生任何关于世界的预言。

