# 交叉一致性审计（2026-08-14 第四轮）

> 触发：用户要求"继续分析整理优化突破"。第三轮已解决 `proof2` 一处内部矛盾；
> 本轮对目录中**尚未审计**的证明脚本（`derivative_proofs.py` / `verify_core.py` /
> `light_core.py` / `verify_uft_repair.py`）做全量交叉口径审计，发现**三层系统性口径冲突**，
> 优先于任何"新突破"。先把矛盾钉死，再谈能突破什么。

---

## 一、三层冲突的精确量化

### 冲突 A：κ/τ 半径口径（与第三轮同源，但旧脚本未纠正）

| 脚本 | 用的约束 | `α=τ/κ` 行为 | 与框架公理 III（`ωρ=c`） |
|---|---|---|---|
| `verify_uft_repair.py`（F3，权威源） | `κ²+τ²=Qtop²`（旋转支，Qtop 恒定） | 沿流 **变化**（`tan u`） | ✅ 自洽：`ω=c√(κ²+τ²)`，`ρ=1/√(κ²+τ²)` ⇒ `ωρ=c` |
| `derivative_proofs.py`（Proof 2） | `κ²+τ²=ω²/c²`（伸缩支） | 冻结为 const | ❌ 与公理 III 排斥（见第三轮判决：会逼 `m_e` 随 ω 漂） |

**判决**：A 支（旋转支）已被第三轮选为框架真实主张。**`derivative_proofs.py` Proof 2 的整个"验证"建立在被否决的 B 支上**，其 PASS 结论无效，但该文件未加撤回标记——属遗留误导资产。

### 冲突 B：α 的两处定义直接打架（报告 §2.2 已标注，本轮实测坐实）

- `alpha_def = τ/κ`，由 `kf=3.162e-4, tauf=2.3076e-6` 给出 `= 1/137.035999`（`derivative_proofs.py`/`verify_core.py`/`light_core.py` 共用这组常数）
- `alpha_geom = Φ_T² = 2⁻⁷ = 1/128`（`verify_core.py` §4.2、`verify_uft_repair.py` N1b）

实测两值之比：

```
alpha_def / alpha_geom = (1/137.036) / (1/128) = 128/137.036 = 0.93408
=> 缺口 = 7.07%（不是单圈能补的量级）
```

**致命点**：`verify_core.py` §5.2 把 `1/128 → 1/137.036` 解释为"Chern-Simimons 单圈跑动修正"，
但 `verify_uft_repair.py` 自己用真实单圈 QED β 函数算出 `α(M_Z)_phys = 1/139.236`（此数字本身有误，见下）。
两个"修正后"目标值（`1/137.036` vs `1/139.236`）**本身差 1.6%**，且：

- 从 `1/128` 出发，单圈跑动实际给 `1/107.966`（**更大**，方向朝**更大**耦合，因 β>0 使 α 随能标增大）
- 实验 `1/137.036` 反而比 `1/128` **小**（方向朝**更小**耦合）
- `1/139.236` 既非真实跑动结果（=1/107.966）也非实验值（≈1/128.9），属旧版误记，已被 `alpha_h1_diagnostic.py`（EXIT=0）证伪

⇒ **单圈跑动无论如何不能把 `1/128` 修正到实验 `1/137`**。报告 §2.2 第 64 行写的
"需统一为 §4.2 的拓扑量化定义（以实验 α(0) 为基线反推）"在 §4.2 里**并未真正执行**——
§4.2 仍写 `Phi_T^2 = 1/128`，与"以实验为基线反推"自相矛盾。

### 冲突 C：三力"整数比 15:4:1"是虚的闭环

`verify_core.py` §4.2 声称 `α_S:α_W:α_EM = 15:4:1` 是推导成果，但同文件 §5.1 自己列出：

| 比 | 框架预测 | 实验值 | 偏差 |
|---|---|---|---|
| α_S/α_W | 15/4 = 3.75 | 0.1179/0.03106 = 3.796 | **+1.2%** |
| α_S/α_EM | 15 | 0.1179/0.007297 = 16.16 | **+7.7%** |
| α_W/α_EM | 4 | 0.03106/0.007297 = 4.254 | **+6.4%** |

且比值建立在 `α_geom=1/128` 这一**错误基底**上（见冲突 B）。整数比只在同一（错误）`Φ_T²` 基底内自洽，
**不是从拓扑推出的可检验预言**。与 `全维独立交叉验证报告.md` V5 把 `15:4:1` 标为"同 Φ_T² 起源，0（代数）"一致——
即它只是代数恒等式，**未与实验闭合**。

---

## 二、被掩盖的真实结构（诚实重述）

把三层冲突剥离后，框架**真正成立**的只是：

1. **公理 III 的代数闭环**（旋转支）：`κ²+τ²=Qtop²` ⇒ `ωρ=c` 精确。✅（Proof 1 / B2）
2. **B 系列其余数学结构**：B1 几何均值导数签名、B3 高维范数障碍、B4 Klein 群谱、B5 纯几何比值。✅（Proof 10–16）
3. **G 的诚实不可生成性**：`G = c·(ℓ_P²c²/ℏ)`，因子 `c` 不独立 ⇒ G 纯拓扑**无法生成数值**，
   只能由宇宙学边界定（结构式 `G=πc³/(S_dS·ℏ·H₀²)`）。✅（已诚实标注）

**真正未破的硬缺口（不可粉饰）**：

- **(H1) α 的数值**：框架给 `Φ_T²=1/128`，实验 `1/137`。缺口 7%，且**方向**与单圈跑动相反。
  任何"推导出 α"的宣称都需先关闭此 7% + 方向矛盾——这是框架当前**最硬的诚实边界**。
- **(H2) 三力绝对大小**：整数比只在错误基底内自洽，不与实验闭合。
- **(H3) G 数值**：宇宙学边界待解（宇宙学常数问题 10¹²⁰）。

---

## 三、对遗留脚本的处置建议（不删除，加标记）

| 文件 | 状态 | 处理 |
|---|---|---|
| `derivative_proofs.py` | Proof 2 建立在被否决的 B 支（伸缩）上 | 顶部加 **RETRACTED** 标记（同 proof2_fix.py） |
| `verify_core.py` §4.2/§5.2 | `1/128→1/137` 的 RGE 解释错误 | 加注：该段与 `verify_uft_repair.py` 单圈结果矛盾，待统一 |
| `light_core.py` | 叙述性，含 `α=τ/κ=1/137` 标定定义 | 加注：此 `α` 是 `alpha_def`，非 `alpha_geom`，二者未统一 |
| `proof2_fix.py` | 第三轮已 RETRACTED | 维持 |

---

## 四、唯一可行的突破方向（下一步）

在 H1/H2/H3 未关闭前，**不应再加"通过"式证明**。可做的**真突破**只有两条诚实路径：

- **路径 P1（收口，非新增）**：把 §4.2 真正改为"以实验 α(0) 为基线反推 `Φ_T_exact=√(α(M_Z))`"，
  并显式**量化** `Φ_T_heur/Φ_T_exact` 的 7% 缺口与方向矛盾——把 H1 从"隐含"升级为"公开未解项"。
- **路径 P2（实突破候选）**：在旋转支口径下，重新求 `α_def=τ/κ` 沿流的**显式流方程**
  `dα/dlnω = sec²(u)`（第三轮已得），并问"是否存在某个拓扑选择的 `u*` 使 `α(u*)=α_exp` 且同时
  满足 `κ(u*),τ(u*)` 与电荷/质量几何定义"——把"选点"从任意调参**降级为拓扑约束求解**。

> 结论：本轮未新增任何"PASS"。审计发现三层口径冲突，其中 B 层（α 两定义打架）是框架**最硬的诚实边界**，
> 必须先行收口才能谈突破。建议下一步执行 P1（公开 H1），再视情况做 P2（把选点变成约束求解）。

---

## 五、P1 已执行（H1 量化公开，`alpha_h1_diagnostic.py`，EXIT=0）

> 新建 `alpha_h1_diagnostic.py` → **EXIT=0**，留档 `alpha_h1_diagnostic_out.txt`。
> 把 §一.B 的"隐含矛盾"升级为**高精度可量化未解项**，不声称任何突破。

### 实测（mp.dps=90）

| 项 | 数值 | 结论 |
|---|---|---|
| `alpha_geom = Φ_T² = 2⁻⁷` | `7.812500000000e-3` = `1/128.000000` | 框架拓扑值 |
| `alpha_exp` | `7.297352569300e-3` = `1/137.035999` | CODATA 实验值 |
| `alpha_exp / alpha_geom` | `0.934061` | 实验比拓扑**小 6.594%** |
| `alpha_geom / alpha_exp` | `1.070594` | 拓扑比实验**大 7.059%**（方向：拓扑耦合偏"大"） |
| 单圈跑动 `1/128 → Z` | `1/107.966` | 跑动使耦合**更大**（更偏离 1/137） |
| 单圈跑动 `1/137 → Z` | `1/117.002` | 同理 |
| 旋转支标定点 `u*=arctan(α_exp)` | `τ/κ=α_exp` 残差 `9.6e-94`；`√(κ²+τ²)=Qtop` 残差 `5.4e-79` | "选点"能容纳实验 α，自动留 Qtop 壳 |

### 判定（诚实）

1. **H1 是 7% 量级硬缺口**：拓扑 `1/128` 比实验 `1/137` **大** 7.06%。方向是拓扑耦合偏强。
2. **单圈跑动不能弥合**：单圈 QED β>0 使 α 随能标**增大**（朝 `1/108` 走），而实验 `1/137` 比 `1/128` **更小**——
   方向**相反**。任何仅含正 β 的跑动都无法把 `1/128` 修正到 `1/137`。
3. **"选点"能容纳但不能预测**：旋转支上取 `u*=arctan(α_exp)` 可使 `α_def=实验值` 且自动满足 Qtop 壳（残差 ~1e-80）；
   但框架**没有给出 `u*` 的拓扑选取规则**——这正说明框架能**装下**实验 α，却**不能从第一性原理推出** α。

### 收口动作（已做）

- `derivative_proofs.py`、`light_core.py` 顶部加 RETRACTED / CONFLICT 标记（见 §三）。
- `verify_core.py` §4.2/§5.2 的 "1/128→1/137 单圈修正" 解释被本诊断**证伪**（方向矛盾），须在报告中标注。
- 主报告 `__全维统一分析报告_20260814.md` §2.2 已标注 H1 为公开未解项。

### 下一步（P2 候选，未执行）

把"选点 `u*`"从任意调参**降级为拓扑约束求解**：在旋转支上写出 `α` 沿流的显式方程
`dα/dlnω = sec²(u)`（第三轮已得），并寻求一个**拓扑齐次的选取规则**（如 `u*` 由某 28 维投影或相位量子化固定），
使 `α(u*)=α_exp` 成为约束的**解**而非**输入**。此为真突破路径，但当前尚无该规则，故不宣称已突破。
