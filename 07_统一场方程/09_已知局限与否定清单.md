# 已知局限与否定清单

[主方程](00_统一场方程.md) · [验证报告](验证脚本/验证报告.md) · [预言与判据](08_预言与判据.md) · [现象覆盖矩阵](07_现象覆盖矩阵.md)

> **本文件是本项目最重要的文件之一。** 一个只列出成功的研究框架不是科学，是广告。以下内容全部是 UFE-1 **做不到**的事。

---

## L1. 中微子质量为零 —— ~~已被实验证伪~~ → **2026-09-19 已修复**

UFE-1 v1 场内容无 $\nu_R$，且 $B-L$ 严格守恒禁止 Weinberg 算符 $(LH)^2/\Lambda$，故原 v1 严格推出 $m_\nu=0$。

**实验**：$\Delta m^2_{21}=7.5\times10^{-5}\ \text{eV}^2$（太阳 + KamLAND），$|\Delta m^2_{31}|=2.5\times10^{-3}\ \text{eV}^2$（大气 + 加速器中微子）。

**修复（2026-09-19）**：按本清单原建议的**最小修复**，加入 **3 个右手中微子 $\nu_R$**（$SU(3)\times SU(2)$ 单态、$Y=0$）⇒ 对全部 5 类规范反常贡献为 0，反常消除不变；经 Higgs Yukawa 给出 Dirac 质量
$$m_\nu=\frac{y_\nu v}{\sqrt2}\ne0,\qquad y_\nu\approx2.9\times10^{-13}\ (\text{由 }\sqrt{|\Delta m^2_{31}|}\approx0.05\ \text{eV 反解}).$$
`verify_core.py` 的 N1 已由 **FAIL 转 PASS**（总 39/2/6）。

**代价（不美化）**：新增 3 个 Dirac 质量 + 3 个混合角 + 1 个 CP 相 ⇒ 参数 **19→26**；
$y_\nu$ 比 $y_e$ 小约 7 个量级，**中微子 Yukawa 的极小性与层级问题仍未解**（与 L3 同源）。

**2026-09-20 自然解释路径（SO(10) see-saw）**：若将 UFE-1 嵌入 $SO(10)$ GUT，$\nu_R$ 可获得大 Majorana 质量 $M_R$，
经 I 型 see-saw 给出 $m_\nu\approx m_D^2/M_R$。此时 $y_\nu$ 可与带电轻子 Yukawa 同量级（$y_\nu\sim y_{e,\mu,\tau}$），
中微子质量微小性由 $M_R\sim10^{3\text{–}10}$ GeV 的大标度解释，**无需极小耦合**。详见 [L15 阶段二](##-l15-真正统一场论的路径2026-09-19-增补)。

---

## L2. 精细结构常数 $\alpha$ 的数值无法推导

$\alpha=g_2^2\sin^2\theta_W/4\pi$，$g_1,g_2,g_3$ 是**输入**。

**必须明确**：任何声称"从统一场论算出 $\alpha=1/137.036$"的宣称，**不属于本理论的内容**。本理论只能给出 $\alpha$ 与 $g_i$、$\theta_W$ 之间的**关系**，不能给出数值。

**2026-09-19 进展（定理 C / D / E，[算法联盟](../04_公共成果/算法联盟_全维自洽与归一化/README.md)）**：把"不能给出数值"从一句立场升级为**可判定的不可行性定理链**——

1. **定理 C（量纲代数路线不可行）**：任意无量纲断言「$T=$ 若干量 $S$ 的组合」必居两情形之一：$S$ 全为已测量量 ⇒ 数值由测量决定（**无预言内容**）；$S$ 含未测量自有量 ⇒ 等式等价于该量的**定义**（不可检验）。
2. **定理 D（旋钮零空间）**：动力学路线中 $\text{nullity}_{\rm dyn}=n-\operatorname{rank}(J_E)$；靶 $q$ 为真预言候选 $\iff dq|_{\ker J_E}=0$。
3. **定理 H（螺旋几何的 α 不可行性）**：对均匀螺旋上 **7 个初等几何作用量**逐一判定——**5 个完全不能固定** $\alpha$（$\text{nullity}_{\rm dyn}=1$），其余 2 个只把 $\alpha$ 固定到**阶 1 值**（$1/\sqrt2$、$\sqrt2$）；且**每一个**的全局极小都落在**边界** ⇒ 最小作用量原理预言的 $\alpha\in\{0,\infty\}$，与观测 $\alpha=1/137.036$ **直接矛盾**。要把 $\alpha$ 钉到观测值，须在作用量中塞入 $O(\alpha^{-2})\approx-6259$ 的调参 ⇒ **仍等价于把 $\alpha$ 当输入**。

4. **定理 F（算子谱路线的可行性 + 规格）**：在螺旋算子类（模量子化 + da Costa 几何势 + 挠率耦合 $c$，扭转边界条件）上，**内点极小存在 $\iff c<-1/2$** —— 定理 H 说几何作用量类**永远没有**内点极小，此处**首次出现能固定 α 的机制**（$\text{nullity}_{\rm dyn}=0$）；此时
$$\alpha^2=\frac{|A+c/2|}{|c+1/2|},\qquad A=\frac{(n+\varphi)^2}{N^2}.$$
即 **砖 (ii) 可行但需近消去**：$A\to-c/2^-$，**残余即 $\alpha^2$**（$c=-1$ 时需 $A$ 精确到 $5.3\times10^{-5}$）。
若 $(m,N)$ 为拓扑整数，则 $\alpha$ 被**真正预言**（$1/\alpha=m\sqrt{2/d}$），但可达集是稀疏 Pell 型序列，**自然区间无一命中 $137.036$**；大绕数的"命中"属丢番图巧合。

5. **定理 G（规格曲线的第一性来源普查）**：$c$ 的几何粒度是 $\frac12\mathbb{Z}$（da Costa 几何势 $1/4$ + Frenet 框架旋转 $1/4$ + 自旋联络）；可行区 $c<-1/2$ 要求 $c\le-1$，而**几何自然值 $c\in\{0,\pm1/2\}$ 全在可行区外**。$A_{\rm req}(c)=\frac{c+1/2}{1+1/\alpha^2}-\frac c2$ 恒为「自然值 $-O(\alpha^2)$」；格点检验（枚举 $m\le2000$）最佳为 $1/\alpha=137.0515$（偏差 $1.13\times10^{-4}$，为观测精度 $1.6\times10^{-5}$ 的 7 倍）。

⇒ **L2 的否定性已加固为结构性结论**：UFE-1 推不出 $\alpha$；**量纲代数（定理 C）与几何作用量（定理 H）在原理上都不可能**；**唯一未封路线是「具非平凡结构的算子谱 + 边界/量子化条件」——定理 F/G 判其「条件可行」**：需超出几何的负挠率耦合 + $A$ 的 $O(\alpha^2)$ 微调。**统一说法：$\alpha^2$ = 近消去残余 = 近似简并的破缺参数。**

---

## L3. 味与质量谱：26 个输入参数

$g_1,g_2,g_3$，$\mu,\lambda$，9 个费米质量，3 个中微子质量，4 个 CKM 参数，4 个 PMNS 参数，$G$ —— 共 26 个（不计 $\Lambda,\theta$；2026-09-19 纳入 $\nu_R$ 后由 19→26）。

跨 13 个数量级的质量谱（$m_\nu\sim0.01$ eV 到 $m_t\sim173$ GeV）**完全由输入给出**。味物理的起源（为什么 3 代？为什么质量如此分层？中微子 Yukawa 为何比带电轻子小 ~7 个量级？）**未解决**。

---

## L4. 层级问题

$$\frac{M_{\rm Pl}}{M_Z}\approx1.3\times10^{17}$$

UFE-1 中 $M_{\rm Pl}$ 与 $M_Z$ 是两个独立输入，**无任何机制约束其比值**。若引力被量子化，Higgs 质量平方的圈修正 $\sim\Lambda_{\rm UV}^2$ 需精细调节到 $10^{-34}$ 量级。**未解决。**

---

## L5. 代数为什么是 3

反常**每代独立**消除，因此第 1、2、3、4… 代在反常上同样合法。

实验排除第 4 代（$Z$ 不可见宽度 $N_\nu=2.984\pm0.008$），但**理论不解释为什么恰好 3 代**。

---

## L6. 强 CP 问题

$\theta$ 项 $\frac{\theta}{32\pi^2}G^A_{\mu\nu}\tilde G^{A\mu\nu}$ 在规范不变性上完全合法，因此**可以**写进作用量。中子电偶极矩实验要求 $|\bar\theta|<10^{-10}$。

**为什么 $\bar\theta$ 这么小？UFE-1 无解释。**（Peccei–Quinn 轴子是主流方案，但轴子不属于 v1 场内容。）

---

## L7. 宇宙学常数问题

观测 $\rho_\Lambda\approx(2.3\times10^{-3}\ \text{eV})^4$。QFT 真空能的量纲估计（以 $M_{\rm Pl}$ 或 $M_Z$ 截断）给出 $10^{55}$ 至 $10^{120}$ 倍过量。

**这是物理学史上理论与实验差距最大的一处。UFE-1 未解决。**

---

## L8. 暗物质

星系旋转曲线、CMB 声学峰、BAO、子弹星系团、引力透镜一致要求 $\Omega_{\rm DM}\approx0.265$。

**UFE-1 v1 的场内容中没有任何暗物质候选者。** 中微子质量太轻、太少（$\Omega_\nu\lesssim0.01$），不足以解释。

---

## L9. 引力未量子化 —— 理论的有效范围

UFE-1 把 $g_{\mu\nu}$ 当作**经典场**。作为有效场论：

$$\mathcal L_{\rm eff}=\frac{R}{2\kappa^2}+c_1R^2+c_2R_{\mu\nu}R^{\mu\nu}+\cdots$$

圈展开的耦合 $c_i$ 不可重整，理论在 $E\sim M_{\rm Pl}$ 处失效。

**明确的有效范围**：$E\ll M_{\rm Pl}=1.22\times10^{19}$ GeV。在此范围内（含全部现有实验）计算可信；超出则不可信。

**未解决**：引力的紫外完备（弦论 / 圈量子引力 / 渐近安全 / 因果集等外部路线见 [05_全球研究](../05_全球研究/02_全球路线/README.md)）。

---

## L10. 耦合常数在单圈不统一

[验证报告 R1](验证脚本/验证报告.md)：SM 情形 $\alpha^{-1}$ 三曲线最小散布 $=3.66$（出现在 $\sim10^{14}$ GeV）；MSSM 对照散布 $=0.049$。

**结论**：UFE-1 v1 **不能**在无新物理的情况下实现耦合统一。这是推动超对称大统一的核心动机之一，本项目如实登记为否定结果。

**2026-09-19 进展（[统一探针](验证脚本/统一探针报告.md)）**：新增 `unification_probe.py`（一环双阈求解器，零依赖），把 L10 从"定性缺口"推进为**定量规格**：

1. **SM 无解**：三条线单环下永不相交（二阈方程行列式 = 0）⇒ 强化 R1.1/R1.2。
2. **结构 no-go（表示论层）**：**一整代矢量费米子对统一"盲"**——其 $\Delta b\propto(1,1,1)$，只平移三线、不改两两之差 ⇒ 加任意代数都不统一。
3. **MSSM（超对称）** 单阈散布 0.049 ⇒ 唯一"自然阈值（~TeV）"下的统一解。
4. **非 SUSY 统一的规格**：固定 $(M_{\rm int},M_{\rm GUT})$ 可解出**所需的 $\Delta b$**（见下表），任何非 SUSY 方案必须命中对应行，且其预言标度被钉死（可被对撞机/质子衰变检验）。

| $M_{\rm int}$ (GeV) | $M_{\rm GUT}$ (GeV) | 所需 $\Delta b=(\Delta b_1,\Delta b_2,\Delta b_3)$ | $\alpha_{\rm GUT}^{-1}$ |
|---|---|---|---|
| $10^{3}$ | $2\times10^{16}$ | (5.47, 0, −4.03) | 10.81 |
| $10^{5}$ | $2\times10^{16}$ | (5.15, 0, −4.07) | 16.13 |
| $10^{10}$ | $2\times10^{16}$ | (3.48, 0, −4.25) | 29.45 |

⇒ L10 **仍未闭合**（UFE-1 v1 不自带统一机制），但已从"不可判"变为**有明确规格的开放问题**。

**2026-09-23 进展（[SO(10) 链探针](验证脚本/SO10链统一报告.md)）**：新增 `so10_chain.py`（零依赖），把上面"规格"落到**写死的内容**上：SO(10) 的三代 $16$ 旋量谱 + 三条极大破缺链（PS422 / R421 / R3221），一环 + 常数匹配 + 简并阈值。

1. **归一化不引用文献**：$k_Y=5/3$、$k_{B-L}=8/3$、三个指标、Higgs 二重态电荷全部由 D5 权重格用精确有理数现场推导，端到端回归重现 $b=(41/10,-19/6,-7)$ 后才允许出结论（**55 项**门禁 PASS：引擎 S0–S5、解 S6、结论性散文同源 S7；含"两步匹配 = 一步匹配"与映射严格仿射。当日为 44 项，$b_2$ 修复后 47 项，加 S7 后 55 项）。
2. ~~**一条真实的否定结果**：双二重体不分裂（情形 C）⇒ $M_{\rm GUT}=5.1\times10^{20}\ \rm GeV>M_{\rm Pl}$，该链在此标量谱下**不物理**。~~ → **2026-09-24 撤销（台账 F4）**：该数值由情形 C 的 $b_2$ **重复计入规范项**产生；修复后重解得 $M_{\rm GUT}=2.293\times10^{16}\ \rm GeV<M_{\rm Pl}$ ⇒ 三条判据全过。**撤销不等于平反**：情形 C 现在只是"与 A/B 同级的条件性解"。
3. **参数计数不是证据**：PS422/R421 各有 3 个未知量拟合 3 个观测量 ⇒ 精确命中是必然。有内容的是**边界**：标度次序 $M_Z<M_R<M_{\rm int}<M_{\rm GUT}$ 只在一段 $M_{\rm GUT}$ **窗口**内成立（两端都会坏），窗口上沿与 3221 链独自解出的 $M_{\rm GUT}$ 恒等（$M_{\rm int}\to M_{\rm GUT}$ 是同一极限，S6.3 验证到 $10^{-12}$）⇒ **三条链只给一个数，不是三重确认**。
4. **唯一的可单点证伪数字**：$SO(10)\to3221\to SM$ 只有 $(\alpha_{\rm GUT}^{-1},M_R)$ 两个未知量 ⇒ 把 $M_{\rm GUT}$ 升级为未知量后方程恰好确定，给出 $M_{\rm GUT}=2.293\times10^{16}$（情形 C）–$1.990\times10^{17}$ GeV（情形 A）、$\alpha_{\rm GUT}^{-1}\in[45.42,\ 47.82]$、$M_R\in[1.30\times10^{10},\ 1.13\times10^{11}]$ GeV。
5. **代价：与 P4 正面冲突**。同一套 $X/Y$ 玻色子必然导致质子衰变，量级标定给 $\tau_p\in[3.4\times10^{35},\ 2.1\times10^{39}]$ yr，而 UFE-1 v1 的 P4 登记为 $\tau_p=\infty$。二者不能同时为真；本探针**不选择，只登记**（见 [08_预言与判据](08_预言与判据.md)）。

⇒ L10 的状态由"未闭合"细化为：**在 SO(10) 内容下，一环统一可解且可证伪，但（a）标量谱的位势与真空方向未知 ⇒ $M_{\rm GUT}$ 有 $8.7$ 倍、$\tau_p$ 有 $6.3\times10^{3}$ 倍不确定度；（b）"统一"与 P4 的质子稳定性互斥。**两环跑动未做；对数阈值的分量级响应已于 2026-09-24 算出（见下文第六项）。

**2026-09-23 第二项进展（[SO(10) 表示论引擎](验证脚本/SO10表示论报告.md)）**：新增 `so10_reps.py`（零依赖，当日 45 项自检全 PASS；2026-09-24 第三项后为 51 项、同日第四项后为 54 项），把上面 (a) 里"标量谱未第一性推导"这条缺口的**表示论部分**补掉：**唯一的李论输入是 D5 的 Dynkin 图**，Cartan 矩阵、正根、$\rho$、Weyl 群阶、维数、Freudenthal 权重重数、$\mathrm{Sym}^2/\wedge^2$ 张量分解、$B-L$ 谱与 $\Delta_R$ 承载判定全部由它推出（张量恒等式由 Adams 运算与 Freudenthal 递推**两条独立路径**互相认定）。在 $\dim\le210$ 内的判定：

1. **NO-GO**：$10_H/\overline{10}_H/16_H/\overline{16}_H/45_H/54_H/144_H$ 里**不存在** $|B-L|=2$ 的态（算得 $\max|B-L|$ 分别为 $2/3,2/3,1,1,4/3,4/3,5/3$）⇒ 无论怎样分支都承担不了 $|\Delta(B-L)|=2$。
2. **$120_H$ 被排除的真正理由**（本阶段最强一条）：它确实含 $(1,1,1)_{\pm2}$，但 $Q=T^3_L+T^3_R+\tfrac{B-L}{2}=\pm1$ ⇒ 一取期望值就**破电磁**。一般地：任何 $(1,1,1)_{\pm2}$ 单态都带 $|Q|=1$，而每个 $\Delta_{L/R}$ 三重态恰有一个 $Q=0$ 分量 ⇒ **"$B-L$ 破而 $U(1)_{\rm em}$ 不破"必然经由 $\Delta_L$ 或 $\Delta_R$**。这把"必须有 $126_H$"从文献引用变成 Cartan 恒等式的推论。
3. **$210_H$ 只承担伴随破缺**：其 $|B-L|=2$ 的 8 个色单态权重全在 $(1,2,2)$ 里 ⇒ 取任一分量都破 $SU(2)_L$，不能充当 see-saw 来源。
4. **最小承载者 = $126/\overline{126}$**，且**两者各自**都同时含 $\Delta_L$ 与 $\Delta_R$（$126$：$\Delta_R$ 在 $B-L=-2$、$\Delta_L$ 在 $+2$；$\overline{126}$ 全反）⇒ 二者之间是**符号选择**，不是"有没有 $\Delta_R$"。
5. **例外通道（不排除，但要付代价）**：$16_H/\overline{16}_H$（以及 $144_H$，同型但更大）可按 $|\Delta(B-L)|=1$ **单步**破 $B-L$ 且保电磁，但每步只带走一个单位 ⇒ 残留 $\mathbb{Z}_3$，且给不出可重整的 $\nu_R$ Majorana 质量；这些分量必是某个 $SU(2)$ 的二重态 ⇒ 单步破缺必然连带破 $SU(2)_{L/R}$。
   （**本句原写**"残留 $Z_2$（物质字称，对质子稳定反而有利）"——2026-09-24 第十一项把"取期望值之后还剩什么"变成荷格上的读数后**推翻**：$B-L$ 荷格由 $16_F$ 定死为 $g_0=1/3$ ⇒ 圆周是 $6\pi$、残留阶 $N=q_\Delta/g_0$，于是 $\Delta(B-L)=1$ 留 $\mathbb{Z}_3$（阶为奇 ⇒ 群里**没有** 2 阶元），而物质字称元 $(-1)^{3(B-L)}$ 在该 v.e.v. 上取 $-1$ 即**被这一步破掉**；含物质字称那个 2 阶元的是 $\Delta(B-L)=2$ 那一步留下的 $\mathbb{Z}_6=\mathbb{Z}_2\times\mathbb{Z}_3$。那个 $\mathbb{Z}_2$ 出自"把圆周当成 $2\pi$"的归一化滑倒（R10.8 把它作为植入缺陷读出）。见报告 §9（R10.0–R10.10）与 08 §3 的 F7。）

⇒ **直接后果**：链探针的情形 B（无 $126_H$）**不能**声称实现了 $B-L$ 破缺，只能读作"跑到 $LR$ 相位"的参考曲线；情形 A/B/C 造成的 $8.7$ 倍摆动里（该倍数已按 $b_2$ 修复后的解更新），至少"哪表示能当 $\Delta_R$"这一项已由计算定死。**仍不闭合**：~~$126_H$ 的完整分支规则系数~~（已由第三项完成）、位势与真空方向、二环跑动与阈值修正未做；且第 2 条用了两个**物理输入**（$Q=0$ 才允许取期望值、$B-L$ 必须被破掉）而非纯李论推导 ⇒ 二者有一条不成立，"$126_H$ 必需"随之失效。L10 状态：**未关闭，但标量内容的允许集合已从"手工放置"收紧为"推导"**（其中"分支规则系数"一项已由下一段完成）。

**2026-09-24 第三项进展（分支规则系数本身）**：`so10_reps.py` 增配极大秩子群分支引擎，自检 **45→51 项全 PASS**。两条破缺链的半单部分都与 $SO(10)$ **共享同一个 Cartan 子代数** ⇒ "分支"就是同一批权重向量换一套标号：3221 的 $A_2\oplus A_1\oplus A_1$ 秩 4，与这四个单根全部正交的那一维**恰好**是 $B-L$；422 把 $SU(3)_c$ 升级为 $SU(4)_c$（$D_3\simeq A_3$，秩 5 = 满秩），Pati–Salam 的 $U(1)$ 自动在 $SU(4)_c$ 的 Cartan 里 ⇒ 该链**没有**独立 $B-L$ 下标。分支重数用 Klimyk–Springer 交错和 $n_\lambda=\sum_{w\in W_H}\det(w)m_\Lambda(w(\lambda+\rho_H)-\rho_H)$，只复用 Freudenthal 已给的 $m_\Lambda$ 与由单根现算的 $W_H,\rho_H$ ⇒ 与 §1 递推**互相独立**。结果（报告 §5）：

1. **完整性**：$\dim\le210$ 的 11 个表示 × 两条链，$\sum_\lambda n_\lambda\dim_H(\lambda)=\dim\Lambda$ **逐项成立**（R6.3）；142 个分量的维数由"整块乘积公式 = 因子乘积公式 = $(p,q)$/$(p,q,r)$ 闭式"三路复核（R6.4）。踩过的坑已固化：$D_3\simeq A_3$ 的单根 $\beta_1=\varepsilon_1-\varepsilon_2,\beta_2=\varepsilon_2-\varepsilon_3,\beta_3=\varepsilon_2+\varepsilon_3$ 满足 $(\beta_1,\beta_2)=(\beta_1,\beta_3)=-1$、$(\beta_2,\beta_3)=0$ ⇒ Dynkin 链是 $\beta_3\!-\!\beta_1\!-\!\beta_2$，标准 $A_3$ 编号 $=(c,a,b)$；槽位放错会把 $SU(4)_c$ 的 $6$ 读成 $4$，而**这种错不会让任何等式变红，只会让物理读错**。
2. **物理锚点（本项目此处唯一引用外部答案）**：$16_F\to(3,2,1)\oplus(\overline{3},1,2)\oplus(1,2,1)\oplus(1,1,2)$，422 侧 $=(4,2,1)\oplus(\overline{4},1,2)$ ⇒ 恰好一整代费米子 $+\nu^c$，UFE-1 的 $3\times16$ 场内容与两条链逐项相容（R6.6）。
3. **与串检验双向一致**：$|B-L|=2$ 的色单态按 $(SU(2)_L,SU(2)_R)$ 归档，分支表与第二项的 $\alpha$-串判据给出**同一个集合**（R6.5）⇒ $\Delta_R/\Delta_L$ 的唯一性由两套独立算法同时命中。
4. **新增可读结论**：$126_H$ 的 422 分支 $=(6,1,1)\oplus(10,3,1)\oplus(\overline{10},1,3)\oplus(15,2,2)$ ⇒ 走 422 链时破 $B-L$ 的标量**必然同时**带一个 $SU(2)_{L/R}$ 三重态（$\Delta_L\subset(10,3,1)$、$\Delta_R\subset(\overline{10},1,3)$）；$(15,2,2)$ 是 $422\to3221$ 那一步的伴随破缺候选。$10_H$ 侧 $(6,1,1)\oplus(1,2,2)$ 的 $SU(3)_c$ 单态分量带 $B-L=0$ ⇒ $\Phi(1,2,2)$ 破不了 $B-L$（链探针 S2.7 的分支规则版本）。
5. **约定提示**：本引擎的 $B-L$ 整体符号与常见文献相反 ⇒ 表中电荷下标只在本引擎内自洽；本引擎 $SU(4)_c$ 的 $4$ 对应文献 Pati–Salam 记法里的 $\overline{4}$。

⇒ **L10 仍未关闭**：分支规则给的是**权重层内容**（哪个多重态、几次、多少 $B-L$），它**不含** Yukawa 系数（哪个拷贝耦合哪一代）、$126_H$ 的位势与真空方向，也不含两环跑动与阈值修正；当时 $SO(10)\to422\to3221$ 的**跨链匹配**只是两列表对照读出的结论，登记为待做的 **R6.7**。

**2026-09-24 第四项进展（跨链匹配成为门禁）**：`so10_reps.py` 自检 **51→54 项全 PASS**。补上子系统的 Freudenthal 权重递推（深度只能用差值 $(\Lambda-\mu,\rho_H)$：子系统 $\rho_H$ 会让 $(\mu,\rho_H)$ 非整，例如 $\rho(A_3)=2\varepsilon_1+\varepsilon_2$）与精确有理线性代数，于是"两步限制 $\mathrm{Res}^{422}_{3221}\mathrm{Res}^{SO(10)}_{422}$"与"一步限制 $\mathrm{Res}^{SO(10)}_{3221}$"能作为 $(SU(3)_c,SU(2)_L,SU(2)_R)_{B-L}$ **多重集**逐项比对：

1. **路径无关性（R6.7）**：$\dim\le210$ 的 11 个表示全部相等。这同时是标签系统的咬合检查——$A_3$ 槽位放错、或 $B-L$ 归一化与链探针不一致，等式当场破，且差额逐条列出。
2. **$B-L$ 的方向唯一性（R6.8a）**：$\mathrm{span}(A_3)$ 中与 $A_2$ 全部单根正交的方向**恰 1 维**，且与链探针的 $B-L$ 方向成比例（本引擎里比例算得恰为 $1$）⇒ $422\to3221$ **不必另外引入 $U(1)$**，那一步的破缺完全由 $SU(4)_c$ 的伴随表示承担。
3. **无迹性（R6.8）**：$\sum_\mu m_\mu(B-L)(\mu)=0$ 在 $A_3$ 的 9 个不可约表示上逐个成立（三个基本表示 + $\sum c_i\le2$ 的全部标号；全程有理数、零浮点容差），同一循环里逐表示核验色分支维数账 $\sum n_\lambda\dim(\lambda)=\dim$。 ⇒ 被破生成元的 $B-L$ 荷（$15\to(3,1,1)_{-\frac43}\oplus(\overline{3},1,1)_{\frac43}\oplus(8,1,1)_0\oplus(1,1,1)_0$）由算出而非引用。

⇒ **L10 仍未关闭**，且未关闭的部分现在**只剩动力学**：两环跑动 + 阈值修正 + $126_H$ 位势与真空方向 + Yukawa 系数，以及"实现统一"与 P4（质子稳定）之间的取舍。权重层到此已无待验证项——第四项把最后一条"由表格对照读出"的结论变成了门禁。

**2026-09-24 第五项进展（一处真实的计算缺陷 ⇒ 台账 F4 撤销）**：与表示论引擎对账时发现 `so10_chain.py` 里情形 C 的 SM 相位 $b_2$ **把规范项 $-(11/3)C_2$ 计了两次**（物质增量被加在已含规范项的表达式上）。自检 44→**47 项全 PASS**：新增 S3.4/S3.5/S3.6 把"情形 C 相对情形 B 的 $b_i$ 增量 = 恰一个二重体的物质贡献、不含规范项"钉成回归门禁。修复后三条情形的解全部移动：

| 情形 | $M_{\rm GUT}$ (GeV) | $\alpha_{\rm GUT}^{-1}$ | $M_R$ (GeV) | $\tau_p$ (yr) | 判据 ①②③ |
|---|---|---|---|---|---|
| A | 1.990e+17 | 47.82 | 1.299e+10 | 2.12e+39 | 通过 |
| B | 5.298e+16 | 46.35 | 4.880e+10 | 1.00e+37 | 通过 |
| C（旧值 $5.1\times10^{20}$） | 2.293e+16 | 45.42 | 1.127e+11 | 3.38e+35 | 通过 |

⇒ **F4 撤销**（见 [08_预言与判据](08_预言与判据.md) §3）。这件事的真正教训不是"数算错了"，而是**那句结论写在报告模板的散文里**：数值翻了一次，表格跟着翻了、"情形 C 非物理"那句话没翻 ⇒ 同一份报告自相矛盾。修法是把理由接到解上（`phys_reasons`）并新增门禁 **S7.1–S7.4e**（含 5 条变异测试：植入每种判据缺陷，理由函数必须逐条报出；S7.2 直接钉住"被判非物理的情形集合 = 空集"），自检 47→**55 项全 PASS**。

**2026-09-24 第六项进展（阈值层：`so10_thresholds.py`，55 项门禁全 PASS）**：把"依赖标量谱"从一句免责声明变成分量级的响应表，并执行 08 登记的待办"补入 $126_H$ 后重跑"（一环口径）。

1. **弹性度** $\partial\ln M_{\rm GUT}/\partial\ln M_j$ 逐 SM 不可约分量算出（切分由权重格自动完成，不是手挑），并与 **36 个完整重解**对账：$\delta\times$弹性度$/\ln10$ 与实测 $\Delta\log_{10}M_{\rm GUT}$ 偏差 $\le10^{-9}$（T7.2，观测最大 $1.0\times10^{-13}$）。最大者 $+0.4610$ —— $126_H$ 的 $(6,3,1)_{Y=-1/3}$；严格为零者 $45_H$ 的 $(1,1,1)_0$ 与 $(1,1,3)_{Y=0}$。
2. **结构结论（T6，精确）**：逐**分量**的 $\Delta b$ 沿三条 SM 方向并不相等，但**整块 $SO(10)$ 多重态**严格相等（$10_H\to1/3$、$45_H\to8/3$、$126_H\to35/3$，合计 $44/3$）⇒ 一块完整多重态放在共同标度上时 $M_{\rm GUT}$、$M_R$ 不动（$10^{-12}$），只有 $\alpha_{\rm GUT}^{-1}$ 移动（$48.7740\to6.1698$，$\Delta=c(t_{\rm GUT}-t_{B-L})/2\pi$ 由 T6.5 复核）。
   ⇒ **"P7 依赖标量谱"精确化为"依赖块内分裂"**；且全套谱都压在 GUT 标度上时 $\alpha_{\rm GUT}^{-1}=6.2$ 已接近强耦合 ⇒ 一环本身在该点失效，这是一条自我限制而非成就。
3. **踩过的两个坑（已固化）**：① 响应表列头一度多乘了一个 $t_j/t_{\rm GUT}$（把 $\partial t_{\rm GUT}/\partial t_j$ 误当 $\partial\ln M/\partial\ln M_j$）——量纲错，由 T7.2 与 §6/§7 交叉对账抓出；② §7 的行筛选条件误用 $Y\ne1/2$，静默丢掉了**所有** $Y=1/2$ 分量（不止轻 Higgs），改为按 `mode != 'light'` 后从 10 行增至 36 行。
4. **给 R9.3 那条缺位定价（报告 §9，T8 族 15 项）**：R9.3 判"$16^{\otimes4}$ 的两个不变张量里恰有一个能经由声明有的 $10_H$ 因子化，另一个需要 $120_H$ 而无人声明"。把 $120_H$ 送进**同一台**分量机器（分支表 + $Y$ 切分 + 整块求和，权重账 120 与引擎独立给出的维数相等）重跑，三条实测：
   **(a)** 整块补入按 T6 机制**不动** $M_{\rm GUT}$ 与 $M_{B-L}$（$t_{\rm GUT},t_{B-L}$ 到 $10^{-12}$ 同值，T8.8），$\alpha_{\rm GUT}^{-1}$ 的移动量被整块求和定点为 $c(120_H)(t_{\rm GUT}-t_{B-L})/2\pi$，$c(120_H)=28/3$、实测 $24.576459$ 对预言 $24.576459$（T8.7/T8.9，相对偏差 $10^{-9}$）；
   **(b) 代价全部落在 $\alpha_{\rm GUT}^{-1}$**：$9.2031\to-15.3734$ ⇒ 一环解离开微扰分支、$\tau_p$ 无定义（T8.10）。只把它的 $(1,2,2)_{0,\,Y=+1/2}$ 留在电弱标度的分裂补法同样出分支，而这次**真的动** $M_{\rm GUT}$：$\Delta\log_{10}M_{\rm GUT}=-0.8583$（T8.11）⇒ "整块不摆动统一点"靠的是**完整多重态**这个前提，删掉它的一块，T6 的公共平移论证当场失效；
   **(c)** 内容上它**给不出** $B-L$ 破缺：$(1,1,1)_{\pm2}$ 沿 Cartan 方向直算的 $Q$ 谱 $=\{-1,1\}$，一中性都没有（T8.5，与引擎 R5.8 同源、本机独立重算）；同时它把谱里电中性弱二重体切片从 4 块加到 6 块，而"哪一块留在电弱标度"在本仪器里是**手工锚定**的（T8.6）⇒ 补入它连"双重 Higgs 二重体"的挑选问题一起带来。
   ⇒ **缺口仍在，但从今天起带着价签**：$120_H$ 不能替 $\overline{126}_H$ 去担 $B-L$ 破缺那一步，而它在阈值层的两种补法都在一环适用范围之外。道分解把它放在反对称槽 $\wedge^2(16)$（T8.14 读自引擎），故"只有它则三代 Yukawa 秩为偶（$\le2$）"——这条是味层算术，本仪器无代自由度，**不进门禁**。
5. **仍不做**：两环跑动（一般群乘积的两环系数矩阵 $b_{ij}$ 需在仓库外核验，本次抓取失败 ⇒ **不凭记忆写**）、有限部分阈值（Goldstone/鬼场、匹配方案差）、$126_H$ 位势与真空方向、Yukawa 系数；分裂超过一步时不追踪中间规范群 ⇒ 响应系数只对中等分裂成立。

⇒ **L10 状态不变：未关闭。**阈值层把不确定度**定位**了（块内分裂，不是"哪个多重态"），但没有把它**消掉**；P7 的分级仍是"条件性、可单点证伪"。三台仪器合起来 **213 项门禁全 PASS**（`so10_reps.py` 103 + `so10_chain.py` 55 + `so10_thresholds.py` 55；其中 4 项由下面第七项、9 项由下面第八项、8 项由下面第九项、6 项由下面第十项、11 项由下面第十一项、11 项由下面第十二项新增，阈值层的 15 项来自上面第 4 条的 T8 族；这些数字由 `维护脚本/build_tables.py` 与仪器 JSON 当场核对，写错即非零退出）。

**2026-09-24 第七项进展（同一失效族在**另一台**仪器上复发：`so10_reps.py` 写死了链探针的结论）**：给 08/09 登记 F4 之后回头扫其余文档，发现表示论引擎的三处散文里写死了 **"$M_{\rm GUT}$ 摆动 $3.8$ 倍"**——那是**链探针的解**，不是表示论的量。$b_2$ 修复把这个倍数从 $3.8\,(A/B)$ 变成 $8.7\,(A/C)$，表格跟着翻了、这三句没翻；且同一文件内部主体还不一致（模块文档串写"A/B 之间"，报告 §0 写"A/B/C"）。这就是第五项那条教训（"结论性散文必须与表格同源"）换了个仪器重演，只是这次跨了仪器边界。

修法与 S7 系列同构，自检 54→**58 项全 PASS**：

1. **接到源头**：新增 `chain_band()` / `swing_note()`，倍数由 `import so10_chain` 当场解出（该文件本就载入了链探针做荷约定的 R2.0 对表，不是新依赖）；§0、§6、模块文档串三处改写为引用该读数。读不到时印"未对账"，**不保留旧数**。
2. **门禁 R2.sw0–R2.sw3**：sw0 要求链探针至少给两条物理解（否则"摆动倍数"无定义）、sw1 要求区间两端来自**不同**情形（同情形当两端 ⇒ 倍数恒为 1 的空话）、**sw2/sw3 是变异测试**：植入假带 $\{A{:}2,\,B{:}17\}$ 必须印出 "8.5 倍——情形 A↔B…"，植入空带必须落到"未对账"分支。⇒ 若有人把 `swing_note` 改回写死字符串，sw2/sw3 立刻变红（已实测：改回写死后两项 FAIL、退出码 1、且 §8 表格仍完整生成）。
3. **顺带修掉一条真实崩溃**：报告生成器在门禁 FAIL 的分支上会 `TypeError: %d format: a number is required, not str`（`g('210','bl2')` 未认定时返回中文串，被 `%d` 吃掉）——即"越是出错越看不到报告"，与 `so10_chain.main()` 里"分开求值再用 `and` 合并"是同一条教训。改为 `gsum()` 数值降级。这条崩溃是**跑变异体时才暴露的**：只看全 PASS 的输出永远发现不了。

⇒ 本项**不**提升任何证据等级：它没有新增物理内容，只是把一个已经被证伪过一次写法的失效族从"链探针内部"扩到"跨仪器"。剩下的仍只有两环、有限部分阈值、$126_H$ 位势与 Yukawa 系数。

**2026-09-24 第八项进展（d=4 Yukawa 通道层：`so10_reps.py` 自检 58→**67 项全 PASS**）**：把"哪些 Higgs 能与物质耦合"从查表变成推导。三条通道全部由 Adams 运算与 Freudenthal 递推**两条独立路径互相认定**地精确拆分（R7.1 自己重算一遍按权重的重构，不信 `decomp` 的返回值）：

1. **通道表**：$\mathrm{Sym}^2(16)=10\oplus126$ ⇒ $16_F16_FH$ 的手征对称通道只受理 $10_H\oplus\overline{126}_H$；$\wedge^2(16)=120$ ⇒ $120_H$；$16\otimes\overline{16}=1\oplus45\oplus210$ ⇒ 向量型（"对消"）通道只有 $45_H\oplus210_H$。两个集合交为空（R7.2）⇒ **没有任何一个表示能同时给出 Dirac 质量与 $B-L$ 破缺**——这不是"还没找到"，而是张量分解的结论。
2. **三条读数**：① 链探针情形 B 的 $\Sigma(1,1,3)_0\subset45_H$ 落在**对消**通道里，而物质恰为 $3\times16$（无 $\overline{16}$ ⇒ 该通道空转）⇒ 它取期望值**不产生任何费米子质量**（R7.7：权重从 `chain.BIDOUBLET/SIGMA_R/SCENARIOS` 现场读，不抄本文件字面量）；② $\nu_R^c$ 在 $16_F$ 里是唯一"色单态 + 弱单态 + $Q=0$"的权重（R7.3），其 Majorana 项所需权重 $-2\nu=(1,1,1,-1,1)$（$B-L=2$、$Q=0$）在 $\dim\le210$ 的 11 个表示里**只**由 $\overline{126}_H$ 携带（R7.4），且它正是该表示 $\Delta_R$ 三重态里**唯一**的 $Q=0$ 成员（R7.5）⇒ "能耦合手征物质"与"能破 $B-L$"这两条独立判据落在同一个表示上：see-saw 不是外塞的机制，而是 $\overline{126}_H$ 取期望值的同一次动作；③ 荷守恒只是**单向**必要条件（R7.6）：$126$ 有 $90/106$ 个权重可加、通道表却不受理 ⇒ 只看权重相加会把它误判成合法 Higgs。
3. **本轮唯一被门禁抓到的真实缺陷**：R7.6 最初对手征行也用了 $16-16$ 的支集算可加性，于是 $126$ 同时出现在"受理"与"可加性过关"两边、(b) 分支空转 ⇒ 可加性必须按**各通道自己的支集**算（手征 $16+16$、对消 $16-16$）。附带修掉两处报告排版缺陷（§6 里嵌套的 `$…$` 未闭合；4 处把 Python 容器 repr 直接印进正文），并为此新增两条全文不变量（段落内 `$` 配平、正文不得出现未格式化对象或 `None`；表格行豁免——那是审计痕迹不是排版），读数随 JSON 一起印出（`report_hygiene`：41 段 / 9 表）。两条防自证的变异测试：R7.8 把成分清单植成 $16$ ⇒ 允许集必须变成 $\overline{16}$（证明共轭映射真在跑）；R7.9 改用同一 $SU(2)_R$ 串的**带电**伙伴 ⇒ 所需权重落到双重带电分量上（证明"$Q=0$ 才允许取期望值"确实在决定结论，不是恒真说法）。

⇒ **本项同样不提升任何证据等级：L10 仍未关闭。**它给出的是**"允许"**（哪些 $d=4$ 算符在群论上存在），不是"自然界选了哪个"；不含 Yukawa 系数与味结构、$126_H$ 位势与真空方向、高维算符（$d=5$ Weinberg 项等）与两环动力学。"允许"与"该场真的存在并取了期望值"之间还差一整个位势。

**2026-09-24 第九项进展（不变张量簿记层：`so10_reps.py` 自检 67→**75 项全 PASS**，报告新增 §7）**：第八项判的是"哪个 Higgs **拿得到** d=4 算符"，材料是双线性拆完再取共轭。本项**故意换算法**判第三问——"哪些组合**本身就是**规范不变的"：直接数 $\mathrm{mult}((1),V)$，同时把两件最容易混的事分成两列：**荷账本** $\dim V_0=m_V(0)$（全部 Cartan 荷为零的方向数，即"荷配得平"）与**不变张量** $\mathrm{mult}((1),V)$（其中真不变的那部分），恒有后者 $\le$ 前者，而**缺口可以逐成分核对**：$m_{A\otimes B}(0)=\sum_Rn_Rm_R(0)$（R8.1a）。

1. **三条读数**：① $(16\otimes16)、(16\otimes\overline{16})、(16^{\otimes4})$ 的（荷账本，不变张量）$=(0,0)、(16,1)、(960,2)$。第一行连"荷"都配不平 ⇒ **手征费米子没有裸质量项**是数出来的（R8.4 另判 $\mathrm{Sym}^2$ 与 $\wedge^2$ 两个槽各自为 0，排除"单态藏在反对称槽"那种假象），且**不依赖** §6 的通道表；② 第三行的 958 个缺口不是丢了，而是记在 $45$ 每个 5、$54$ 每个 4、$210$ 每个 10 的 Cartan 子空间上（另有 8 条 $\sum c_i>3$ 的**未登记**标号占 836，本节给它们不起名字）⇒ "荷守恒"比"不变"弱多少是可数的；③ 单态 $(1)$ 的权重集就是 $\{(0,0,0,0,0)\}$，五个 Cartan 方向上的荷全为 0 ⇒ **任何物质-only 规范不变算符自动 $\Delta(B-L)=0$**；同时 $\mathrm{mult}((1),16^{\otimes4})=2>0$ 而 $\mathrm{mult}((1),16\otimes16)=0$ ⇒ 规范群**不**禁止四费米子算符（质子衰变那一类的荷前提成立）却禁止二费米子那一种；要拿 $|\Delta(B-L)|=2$ 必须插 Higgs，而同一个计数在 $\overline{126}$ 上给 1、在 $126$ 上给 0 ⇒ 插的是**带共轭杠**的那个，与第八项 R7.4/R7.5 同源而路径独立。
2. **四条路径必须咬合同一个数**（R8.0/R8.2）：先现场核验不变张量基本定理 $\mathrm{mult}((1),A\otimes B)=\delta_{B,\bar A}$——$\dim A\cdot\dim B\le2000$ 的全部 52 个已登记表示对，左边由 `decomp` 解方程、右边由共轭标号，**两边不共享代码**（$\delta=1$ 恰 6 对、反例 0）⇒ 配对和那条路的查表规则是验证过的而非引用；再对 $16\otimes16\otimes\overline{16}\otimes\overline{16}$ 走四条路（两种结合方式各解一次方程 + 手征侧／对消侧各配一次和）$=[3,3,3,3]$。顺带把**结合律本身**做成可核对的事：两种结合方式的权重多重集**逐权相等**（1531 个不同权 / 65536 个权重含重数）。$\overline{16}$ 全换成 $16$ 后配对和从 3 掉到 2（$126$ 找不到 $\overline{126}$ 作伙伴）⇒ 共轭在这一步真的在起作用。
3. **§6 通道表由第二条路独立复算**（R8.3）：逐个 Higgs 候选数 $\mathrm{mult}((1),\text{双线性}\otimes H)$ 是否 $>0$，与 R7 的成分表按**标号**（非名字，$10\sim\overline{10}$、$45\sim\overline{45}$ 同标号）比对通过。变异测试 R8.6 三处植入：去 1 个零权 ⇒ 合法退化为 $45\oplus210$、单态 1→0；去 2 个 ⇒ 不再是特征标的非负整组合，**整权重构对账必须拒绝**（不是沿用旧答案）；换 $16$ ⇒ 1→0。
4. **踩到的两条元层缺陷（比结果更值得记）**：① 报告排版门禁原来只有两条不变量（`$` 配平、无 repr 泄漏），而本轮真实踩到的是第三类——`%` 只绑到**拼接字面量的第一段**、或漏写 `% V` ⇒ 占位符原样印进正文，**没有任何门禁会变红**。补为第三条（正文无未替换的 `%d`/`%(name)s`）之后，**该断言自己的消息串就犯了它要拦的错**（`% 参数` 未转义 ⇒ 命中时抛 `ValueError` 而不是可读消息）。② 更阴的一次：植入式核验的**第一版是假绿的**——把引擎复制到账本目录跑，那里没有兄弟脚本 `so10_chain.py` ⇒ R2.0 未过 ⇒ R7/R8 整层不运行 ⇒ §7 整段降级 ⇒ 植入的占位符**根本没进正文**。看起来"检查没抓到缺陷"，其实是"缺陷没被送到判据面前"。补齐兄弟脚本后：对照 EXIT=0、两处植入各被抓到 ⇒ **核验一条防护，要先核验它真的在接收流量**。
5. **成本**：整机 1.0→7.4 s（R8 一层 ~5.3 s，其中 36 个三重积拆分 ~4.5 s）。为不把同一拆分算七遍，`singlet_mult` 按**多重集内容**记忆（变异体各得其条目，不会互相污染）。

⇒ **L10 仍未关闭，本项不提升任何证据等级。**它数的是**规范不变张量的个数**，不是算符个数：Lorentz 指标、代（味）指标与 Fierz 恒等式三关都没过 ⇒ "$16^{\otimes4}$ 有 2 个不变张量"**不等于**"有 2 个质子衰变算符"，这些数**不**回填进任何寿命计算（$\tau_p$ 只由链探针口径给出）；$\Delta(B-L)=0$ 是**荷**的陈述而非重子数（同样的不变张量完全可以 $\Delta B=\pm1,\Delta L=\pm1$）；"不变"是群论陈述，**不**等于自然界里有该算符。两环跑动、$126_H$ 位势、Yukawa 系数与味结构全部仍在原处。

**2026-09-24 第十项进展（不变张量的道分解层：`so10_reps.py` 自检 75→**81 项全 PASS**，报告新增 §8）**：第九项数出了 $\mathrm{mult}((1),16^{\otimes4})=2$，但没回答"这 2 个**各自**经由哪条双费米子道"。本项**不换算法、只换粒度**——把同一条配对和 $\mathrm{mult}((1),A\otimes A)=\sum_Rn_Rn_{\bar R}$ 按道 $R$ 拆开 ⇒ 每一道要么整体计入（$R$ 实，此时 $\bar R$ 就是它自己），要么整体不计（$R$ 复且 $\bar R$ 不在成分表里）⇒ 读到的是"**哪一条**"，不是"有几个"。

1. **道表与两侧的合计**（R9.0/R9.1）：$16\otimes16$ 拆成 $10$、$120$、$126$（各 $n_R=1$）⇒ 该道贡献 $10\to1$、$120\to1$、$126\to0$，合计 2 = 第九项解方程得到的那个 2；$16\otimes\overline{16}$ 的 $(1)$、$45$、$210$ 全是实表示 ⇒ 三条道各贡献 1、合计 3。$126$ 是六条道里**唯一"在场却不闭合"**的那条，而它正是唯一能承载 $\nu_R$ Majorana 质量的 $\overline{126}_H$ 的伙伴 ⇒ 同一张道表里同时读到"物质-only 四费米子不变张量走哪两条道"与"$|\Delta(B-L)|=2$ 那条道为什么进不来"；而 $\overline{126}$ **已在登记表里** ⇒ "它不在 $16\otimes16$ 里"是真的落空，不是查不到名字。每条道的闭合判据各走两条不共享代码的路（标号自共轭 vs 直接解 $\mathrm{mult}((1),R\otimes R)$）且逐条一致。
2. **两条闭合道不在同一个双线性槽里**（R9.2）：$\mathrm{Sym}^2(16)=10\oplus126$、$\wedge^2(16)=120$（槽由 Adams $\psi_2$ 算出，与直接做 $\chi_{16}^2$ 的那条路不共享代码，两槽逐权重相加恰为 $16\otimes16$）⇒ 那两个不变张量一个来自"对称槽 $\times$ 对称槽"、一个来自"反对称槽 $\times$ 反对称槽"；同在对称槽里的 $10$ 与 $126$ 一个闭合一个不闭合 ⇒ 不存在"对称槽里都闭合"这种捷径。此处 Sym/∧ 说的是 $SO(10)$ 指标槽的对称性，**不是** Lorentz 或味。
3. **插入 Higgs 的能与不能**（R9.4）：单个 $126$ 或 $\overline{126}$ 插进 $16^{\otimes4}$ 后单态仍为 **0**——三种结合方式（先配四费米子再插、把 $H$ 并入任一双线性、逐道 CG）各算一遍全为 0，9 个道对里可直解的 5 个逐道给 0（另 4 个超出 $2\times10^{5}$ 个权重的成本上限 ⇒ 只报"未直解"，不报结论）。**成对**插入才非零：$126|126$、$\overline{126}|\overline{126}$、$126|\overline{126}$ 按结合 I／II 各给 $12|12$、$21|21$、$20|20$，而共轭镜像（全部 $16\to\overline{16}$、$126\to\overline{126}$）给出 $21,12,20$ ⇒ 镜像与第 2 列交叉相等，它不是一条读数抄两遍。
4. **本层第一次把配对和改走标号路径，起因是一条真实的"视力"缺陷**（R9.5 第四条）：登记表只覆盖 $\dim\le210$，"按名字查共轭"在登记范围外会把**真实存在**的共轭伙伴静默算成 0。$16^{\otimes4}\otimes126^{\otimes2}$ 这张表有 13 条未登记标号，按标号算得 **12**、按名字查得 **3**；反向核验同批在场——第九项 R8.2 用过的那三张表（没有未登记标号）两条路给出**同一个数** ⇒ 旧读数没有被改，只是它的**适用边界**现在被量出来了。留下的规矩：凡引用登记范围外标号的共轭配对，一律走标号路径。
5. **与链探针声明的标量内容对账，分辨"输入"与"结论"**（R9.3）：$\Phi(1,2,2)$ 的全部 4 条权重同时落在 $10$、$120$、$126$ 三条道的权重集里，$\Sigma(1,1,3)_0$ 的全部 3 条同时落在 $45$、$210$ 里 ⇒ 权重包含判据能分"手征侧 vs 对消侧"、**分不了同侧内部是哪一道** ⇒ "道 $\to$ 母表示"的指派是模型的**输入**（写在情形文字里），不是权重算出来的结论。按那份输入数：三个情形声明的母表示并集为 $10_H$、$45_H$ ⇒ 两条闭合的手征道里恰有 $10$ 在谱里有母表示，另一条需要 $120_H$ 而**无人声明**。
6. **变异测试四处植入**（R9.5）：删掉 $120$ ⇒ 2→1；把闭合道 $10$ 的重数**翻倍** ⇒ 该道贡献按 $n_Rn_{\bar R}$ 是 $2\times2=4$ 而非 2，总数 2→**5**（若配对和只数"有没有"，翻倍不会改变任何数 ⇒ 这一条同时钉住"配对和双线性地依赖成分表"）；伪造 $\overline{126}$ 也在 $16\otimes16$ 里 ⇒ $126$ 与 $\overline{126}$ 互配各得 1，2→4；真值 2 与前三条同批打印。
7. **审计 §8 时踩到的第 4 条排版不变量（工具层，不是物理层）**：`table_row_safe` 把表格里的裸竖线改写成 `\lvert/\rvert` 时不留分隔符，而 TeX 按**最长匹配**切控制词 ⇒ 报告里当时落下 20 处未定义控制词命中、共 4 种（`\lvertB`、`\lvertW`、`\lvertQ`、`\lvertD`），整段公式渲染报错，而门禁**全绿**（当日 81 项，同族前三条各修掉一处同类"只错位不变红"的缺陷）。修法是补回分隔空格：数学模式里的空格不参与排版 ⇒ 只修 token 边界，改前后逐行比对确认全篇 418 行里只有 19 行变动、且每一行的差异都恰好是"多一个空格"。并把"全文 `\lvert`/`\rvert` 后不得紧跟字母或数字"做成**全文**断言（前三条只查正文段落、表格行豁免——那是审计痕迹；这一条反过来**不**豁免，因为缺陷恰恰只出现在表格里），读数进 `report_hygiene.delimiter_munch`（当前为空，与段落数 66、表格数 16 一起打印）。这条防护自己也被做成**可重跑的取证**：`维护脚本/check_placeholder_guard.py` 现有 22 例固定样例，其中 6 例专打它——把刚补上的分隔空格再摘掉，不变量必须在同一行上命中；对磁盘上那份真报告要求"确有分隔符（JSON 的 `live_separators` > 0，数目只在那份报告里，本句不抄）且 0 处命中"（只要求 0 命中是空转，必须先证明它有流量）；再把同一次摘除做到**整份报告**上，要求命中数非零且"命中 + 摘掉后仍安全 = 全文分隔符总数"这条账成立。**上一段那句"修好前留着 20 处"因此不再依赖任何人的记忆**：修法是纯空格的，摘掉空格 = 逐字节回到修好前，脚本每次重跑都现算出那个数（**此刻**的读数是 21 处命中 / 4 种：`\lvertB` 14、`\lvertD` 1、`\lvertQ` 2、`\lvertW` 4，且 $21+65=86$ 与全文分隔符总数对上 ⇒ 报告自那条记账以来又长了新行，这个数就跟着长，四类一种未变 ⇒ **它属于报告，不属于句子**，引用它必须连当天那份 `check_placeholder_guard.json` 一起引）（本仓库登记过的另一族正是"报告 .md 未入 git、见证文件只在临时目录里"⇒ 一句话没人能重跑，就等于没人看守）。同一轮里还抓出一条**元层**的：`check_docs()` 的"过去时/阶段标记豁免"粒度是**整个段落**，于是 README 里一句为了交叉引用而写的"下面第十项…"把它那 921 字符段落里的"门禁合计 176""防护例数""缺陷处数"三处现在时数字**同时免检**了 —— 植入测试当场表现为"改了数字也没人报"。修法是把这两族（例数、命中处数）移出豁免：它们的读数每次都能现算，"过去时"在这里没有合法含义；并把那一次误豁免本身固定成第 15~18 例（改错必须被抓、加记号且数字对时不得误报、加记号且数字错时仍须被抓）。这是这一族第四次"先有真实缺陷、后有不变量"。

⇒ **L10 仍未关闭，本项不提升任何证据等级。**本层判的是"规范不变张量经由哪一条道"，仍是群论陈述：四个 $16_F$ 落到真实拉氏量还要过 Lorentz 指标、代（味）指标与 Fierz 恒等式三关 ⇒ "某道闭合"**不等于**"有一个算符"，这些数**不**回填进 $\tau_p$ 的任何计算；"闭合的道里有没有媒介者"依赖手工放置的标量谱与情形文字里的指派；"单个 $126$ 插不进 $16^{\otimes4}$"只说**这一个**乘积里没有不变方向，**不**否定 $|\Delta(B-L)|=2$ 的过程存在（传播子、$\langle\Delta_R\rangle$ 的插入与有效算符匹配都在本层之外）。两环跑动、$126_H$ 位势、Yukawa 系数与味结构全部仍在原处。

**2026-09-24 第十一项进展（残留离散规范对称层：`so10_reps.py` 自检 81→**92 项全 PASS**，报告新增 §9）**：前十项判的都是"哪个分量**能**取期望值、哪个 Higgs 拿得到算符"，本项判下一问——"取了之后 $U(1)_{B-L}$ **还剩**什么"。这一问此前只存在于散文里，而本项的第一个动作是**推翻它**：报告与本文都写着"$|\Delta(B-L)|=1$ 的单步破缺 ⇒ 残留 $Z_2$（物质字称，对质子稳定反而有利）"，可守这句话的门禁 R5.9 读的是 $(B-L,\ \mathrm{SU}(2)_L\ \text{串长},\ \mathrm{SU}(2)_R\ \text{串长})$ 三元组 ⇒ **"残留群"这三个字从未被算过**。

1. **荷格把圆周定死**（R10.0/R10.1）：$16_F$ 的 $B-L$ 值集合是 $\{-1,-1/3,1/3,1\}$ ⇒ 其 $\mathbb{Z}$-生成元 $g_0=1/3$（分子 gcd／分母 lcm，0 跳过），把 $\dim\le210$ 表内 11 个表示全部加进来 $g_0$ **仍是** $1/3$ ⇒ 周期不是"选了哪个标量谱"的函数；于是 $U(1)_{B-L}$ 作为**作用在这些在场态上的**圆群，周期 $\alpha_0=2\pi/g_0=6\pi$，残留阶 $N=q_\Delta/g_0$。第二条路独立地**量**出这个周期：把圆周均分后逐点数使 $e^{i\alpha q_\Delta}=1$，扫出的最小平凡角恰为 $6\pi$ ⇒ 归一化不是抄来的。
2. **读数与三条路径**（R10.2/R10.7）：$q_\Delta=1\Rightarrow N=3$、$q_\Delta=2\Rightarrow N=6$，甲（精确有理）与乙（浮点几何枚举）逐候选相等；丙把全套荷同乘 $1/2$（改用 $Y_{B-L}=(B-L)/2$ 的记号）后 $N$ **一个都不动** ⇒ 残留群是一个群的子群，不是记号的函数。
3. **旧散文错在哪、那个 $\mathbb{Z}_2$ 从哪来**（R10.5/R10.8）：$N=3$ 是**奇数阶** ⇒ 群里根本没有 2 阶元可言，而物质字称候选元 $(-1)^{3(B-L)}$ 在 $q_\Delta=1$ 那个 v.e.v. 上取 $-1$ ⇒ 物质字称是**被这一步破掉的**，不是它留下的；含 2 阶元的是 $q_\Delta=2$ 那一步留下的 $\mathbb{Z}_6=\mathbb{Z}_2\times\mathbb{Z}_3$（那个元在 $\alpha=3\pi$，即群参数 $k=N/2$）。错值的出处被做成**两种植入缺陷**读出来：(i) 误把圆周当 $2\pi$（即默认荷格是 $\mathbb{Z}$）⇒ 残留阶退化成 $q_\Delta$ 本身，$\Delta(B-L)=2$ 那一条就被读成 $\mathbb{Z}_2$、$\Delta(B-L)=1$ 那一条被读成平凡——旧散文正是拿了前者的数、挂到后者上；(ii) 只把 $q_\Delta$ 减半而不同步缩荷格 ⇒ 腰斩，或根本不是整数（工具**拒绝给数**，印成"无解"）。
4. **残留群那两个子群各自是谁**（R10.3/R10.4）：2 阶元作用在荷 $q$ 上是 $(-1)^{3q}$，它在 $16_F$ 每条权重上取 $-1$、在 $10,\overline{10},45,54,120,126,\overline{126},210$ 每条权重上取 $+1$ ⇒ 它就是"物质 vs 标量＋规范"的**物质字称**，而例外（$16,\overline{16},144$ 含 $3(B-L)$ 为奇的分量）由数据给出 ⇒ "标量一定偶"是**算出来**的；3 阶元只看 $3q\bmod3$，实测同余式 $3(B-L)\equiv s\,(p-q)\pmod 3$（$(p,q)$ 是该权重的 $A_2$ Dynkin 标号，符号由数据选）在全表每条权重上一致 ⇒ 它就是按色三重态计数 mod 3 的那个 $\mathbb{Z}_3$（文献称 triality $=$ 命名，不属本节判定）。
5. **一条 iff 与一条"躲不掉"**（R10.6）：物质字称存活 $\Leftrightarrow$ 残留群含 2 阶元 $\Leftrightarrow$ $N=q_\Delta/g_0$ 为偶 $\Leftrightarrow$ $3q_\Delta$ 为偶，逐个候选核对（把 $mp\_parity$ 的奇偶判定与 $residual\_order$ 的格算术这两个**独立实现**拴在一起，而不是拿"偶数阶群必有 2 阶元"那句代数当结论）；同时每个候选的 $N$ 都被 3 整除 ⇒ **在这个物质内容下 triality 是躲不掉的**，条件性的只有 2 阶元那半边。
6. **弱电那一步不再削弱它**（R10.9）：表内"色单态、$Q=0$、$B-L=0$"的候选非空（$10,\overline{10},54$）⇒ 用这类分量再破 $\mathrm{SU}(2)_L\times U(1)_Y$ 时 v.e.v. 对 $U(1)_{B-L}$ 恒等，整个 $\mathbb{Z}_N$ 一路活到标准模型群。这条判据配了一个**必须变小**的对照（再取 $q_\Delta/2$ 之后活下来的元数严格少于 $N$），否则它就是"$0\cdot\alpha=0$"那种恒真式。
7. **两个残留群都是条件读数**（R10.10）：承载 $q_\Delta=1$ 的 $16_H/\overline{16}_H/144_H$ 与承载 $q_\Delta=2$ 的 $126_H/\overline{126}_H/210_H$，与链探针三个情形**字面声明**的标量谱交集为空（同 R9.3 的判据口径）⇒ 本节给的是"若走这条通道则残留 $\mathbb{Z}_N$"，不是"自然界有这个离散规范对称性"。报告 §9 的每个数字都由插值取自读数字典 `RESID`（含"11 个表示"、$6\pi$、$\mathbb{Z}_6$）⇒ 改荷格就改文字，无一处手抄；`main()` 亦把 g0/周期/候选表/字称/交集打印出来。

8. **审计 §9 时踩到的第 5 条排版不变量：表格里"没被转义"的竖线会凭空多出一列（工具层，不是物理层）**：GFM 的表格单元格里，竖线只有被**奇数**个反斜杠紧挨着才算转义 ⇒ 写两个反斜杠 = "一个字面反斜杠 + 一根照常分裂单元格的竖线"⇒ 该行多出一个不存在的列，整张表渲染错位，而全部数值门禁照绿。本轮它被抓在**五处现场**（`build_tables.py` 的新核对逐行点出"这一行的分裂竖线数 vs 其表头"）：05 的 R1g 那一行 7 条 vs 表头（同一张推导表 :286）5 条、09 的第十项结论表一行 6 条 vs 表头 4 条、09 的 L10 台账那一行 7 条 vs 表头 5 条，以及 `SO10阈值报告.md` 的 T8.5/T8.6 两行各 9 条 vs 表头 7 条。前四处的写法是把绝对值直接写成裸竖线（修法是改用 `\lvert … \rvert`，与第四条同族 ⇒ 后面必须留分隔空格）；**后两处的根因在生成器里**：`write_report()` 把"转义竖线"写成了 `'\\\\|'`（两个反斜杠），于是一份机器生成的门禁表自己带病，而写它的 `so10_thresholds.py` 里 T8 族全 PASS。**先取证、再修**：修好前那五行就是第 19~22 例的被检对象——把一处改回裸竖线、再把另一处写成两个反斜杠的"转义"，`check_tables()` 必须各点出被投放的那些行，而**一个**反斜杠的合法转义不得报（否则它数的是竖线，不是**没被转义**的竖线）；被点的行号由被检文件自己算出，不由写样例的人记得的行号说出（写死"1 处"的第一版在同一次运行里就被 09 的两处锚点证伪成**假红**）。判据只判列数、不判内容，扫描范围 = 5 份手写文档 + 2 份生成的表 + 3 份生成的报告（表数与行数由 `main()` 每次运行末尾打印，不抄进文档）；它与门禁数核对**合并在同一次退出码**里 —— 先返回门禁数的那一版会把列数缺陷整个藏进下一次运行，正是本仓库登记过的 fail-open 族。**两条边界如实登记**：(i) 同一根因的第二种伤**不**被本条抓住——同一份报告 §3 的表头 `$3\\times16$` 也多打了一个反斜杠，可它后面不是竖线 ⇒ 列数没变，是读改动时才看见的（它同时是"为什么连表头一起修"的出处）；(ii) `SO10表示论报告.md` 里由 Python repr 转储出的注记单元格自带成对反斜杠（`$\\Phi\\subset$` 一类），同样不分裂单元格 ⇒ 本条不覆盖，已另立待办。这是这一族第五次"先有真实缺陷、后有不变量"。

⇒ **L10 仍未关闭，本项不提升任何证据等级**，且它**降低**了一条既有断言的可信度（那句"残留 $Z_2$"被记为 08 §3 的 F7）。边界有四条：本层只算 $U(1)_{B-L}$ **这一个因子**内的残留子群，$SU(3)_c$ 与 $SU(2)_{L/R}$ 的中心、以及规范群的整体形式（$Spin(10)$ 与它的商）都不在那张荷格里 ⇒ 完整残留群可能是这里那个 $\mathbb{Z}_N$ 的扩充；走 422 链时 $B-L$ 不是独立因子（它坐在 $SU(4)_c$ 的 Cartan 里，见 §5 的 R6.7）⇒ 那条链上的残留要按 $SU(4)$ 权格重做，**本层未做**；残留群在低能**禁掉哪些** SM 不变算符（$u^cd^cd^c$ 一类的字称）未算 ⇒ 已登记为待办（08 的 P7），故本层不回填质子寿命、也不回填暗物质论证；量子引力与阈值层无关，L10 的原缺口（位势、真空方向、两环、Yukawa 系数与味结构）一律原样保留。

---

**2026-09-24 第十二项进展（低能算符选择定则层：`so10_reps.py` 自检 92→**103 项全 PASS**，报告新增 §10）**：第十一项数到"残留群里有哪个元"为止，并把"它能禁哪些算符"登记为待办（08 的 P7 一节末尾）；本项做那一问，用的仍是同一台仪器、同一条 $Y$ 切片上的权重格，不引入任何外部表格。判据被压成**一条同余式**：总荷 $Q=\sum_k(B-L)_k$ 的算符在 $\mathbb{Z}_N$（$N=q_\Delta/g_0$，$g_0=1/3$ 沿用 R10.0 的读数）下不变 $\iff$ $N\mid 3Q$。

1. **projector 先要选对**（R11.0）：$SU(2)_L$ 的单根与 $Y$ 正交（内积 $=0$）而 $SU(2)_R$ 的单根与 $Y$ **不**正交（$=144$）⇒ $U(1)_Y$ 与 $SU(2)_R$ 混在一起，沿 $3221$ 数"零权"等于在荷守恒之外多要 $T^3_R=0$ 与 $B-L=0$。后果正落在最要紧的场上：$16_F$ 的零权方向 **0** 个，而它有 **1** 个 SM 单态，即 $\nu^c$（3221 行 $(1,1,2)_{-1}$、引擎号 $B-L=-1$）⇒ 全部低能计数改在 $Y$ 切片上做（子系统正根 4 条、Weyl 群 12 元、$\rho_{321}=(12, 0, -12, 6, 6)$）。这条不是装饰：把"先按 $Y$ 切片"去掉、直接对整个多重集作同一条 Klimyk 交错和，$10/16/45/54$ 四本维数账**全部**立刻不平（$\sum n\dim$ 读成 40/96/405/486，真值 10/16/45/54）⇒ 维数账能看见切片（R11.1）。
2. **场清单与荷账是量出来的**（R11.3/R11.4）：$16_F$ 展开成 6 条低能场，每条 = 一个 3221 行 × 一个 $Y$ 切片、重数 1，且这 6 条的权重**恰好分成** $16_F$ 的 16 条权重（多重集相等，不只是维数相加）；名字由 $(\lambda,Y,B-L)$ 三元签名绑定（只按 (色,弱) 定名会把 6 条读成 4 类）。$B$、$L$ 不外部填入，由一条规则从 (色, $B-L$) 派生（色非单态者 $B=B-L$、$L=0$；色单态者 $L=-(B-L)$、$B=0$），该规则在 $16_F$ 里反例 0 条，派生的 $(B,L)$ 多重集与教材一代六个场**比对**相同（比对 ≠ 推导，与 R6.6 同一处置 ⇒ 同时植入变异体：把"哪一半携带 $B$"对调，多重集立刻不等）。6 条场的 $3(B-L)$ **全是奇数** ⇒ §9 那个 2 阶元在低能就是物质字称。
3. **$\mathbb{Z}_3$ 的荷完全由色携带**（R11.2）：把 R10.4 那条同余从权重搬到场上，对表内 11 个表示展开出的 166 条低能场逐条核 $3(B-L)\equiv s(p-q)\pmod 3$；**符号是测的**：两套荷号 × 两种符号共四种配对全跑，教材号唯一一致 $s=+$（配 $-$ 反例 98 条）、引擎号唯一一致 $s=-$（配 $+$ 反例 98 条），而 R10.4 在权重层读到的是 $-$ ⇒ 两层是**同一条**同余，差的恰是 R5.7/R6.6 登记的那条"教材号 $=-$ 引擎号"约定差。分档：triality $0$：68 条、$1$：49 条、$2$：49 条 ⇒ 非平凡的那一半全是色三重态。
4. **三条不共享算术的计数路径必须同数**（R11.5）：(i) 逐条场组合算 SM 单态再乘有序组数、(ii) 直接对 $16^{\otimes n}$ 的整权重乘积作 Klimyk、(iii) 先在 SO(10) 层拆不可约（R8 那台独立机器）再逐表示投影。读数 $n=2$ 为 $1,1,1$（21 个场内容类、SO(10) 侧 3 个不可约），$n=4$ 为 $117,117,117$（126 类、12 个不可约）⇒ "按场内容分类"与"按 SO(10) 道分类"是同一件事的两种数法。
5. **判决表与那一问的答案**（R11.6–R11.8）：两条物质场相乘里含单态的只有 $\nu^c+\nu^c$（单态数 1、$\Delta B=0$、$\Delta L=-2$、$3Q=6$）⇒ 物质双线性里唯一的低能不变算符就是右手中微子的 Majorana 项，两条通道下都**允许**。四条物质场（$d=6$）的 126 个场内容类里含 SM 单态 8 条（合计 10 个不变张量结构），逐条按 $N\mid 3Q$ 判 ⇒ **允许 8 条、禁戒 0 条**；其中 $\Delta B\ne0$ 且 $\Delta L\ne0$ 的 3 条（$e^cd^cu^cu^c$、$\nu^cd^cd^cu^c$、$QQQL$）$\Delta(B-L)$ 全为 0 且**全部允许** ⇒ **残留离散规范对称性不保质子**。机理两半边各量一次：同余式在 126 条 d=6 候选上与色侧和模 3 不同余的有 0 条（两台机器在跑同一条判据）；$\mathbb{Z}_3$ 判禁的 81 条里含单态的 0 条 ⇒ 它在低能单态层**没有**额外信息；$3Q$ 为偶的 126/126 条 ⇒ 物质字称那半边在 d=6 无可禁之物（但下面插入奇荷标量的候选正是被它判禁的）。
6. **"禁 0 条"与"枚举到 0 条"必须可区分**（R11.9）：(i) 候选非空（d=6 类 8 条、双线性 1 条、插入**字面声明**谱 $10_H\oplus45_H$ 的 d=4 候选 17 条）；(ii) 换插入 $3(B-L)$ 为奇的标量（$16_H\oplus\overline{16}_H\oplus144_H$ 型）⇒ 候选 40 条、$\mathbb{Z}_6$ **判禁 40 条**；(iii) 把 $\mathbb{Z}_6$ 误读成 $\mathbb{Z}_3$ ⇒ 同一批上 40 条判决差异；(iv) 反向对照：整套荷换号时判决翻转 **0** 条（这一条**必须**为 0，否则结论是挂在记号上而不是荷上）。另有一条**双向**反例专门钉住错投影：那 8 条里它读出 0 的有 1 条（$\nu^c\nu^c\nu^c\nu^c$ ⇒ 漏），读得比单态数大的另有其条（$QQQL$：36 $>$ 2 ⇒ 多算）⇒ 零权数与单态数之间**没有**不等式关系。
7. **条件性原样继承 §9/R10.10**（R11.10）：$q_\Delta=2$ 通道的母表示 $126_H/\overline{126}_H$ 不在链探针字面声明的谱里（声明的只有 $10_H\oplus45_H$，与承载者的两个交集全为空）⇒ "只按声明谱"那一步里残留群根本还没出现，那里的被禁条数是"无从谈起"而非物理数；把 $126/\overline{126}$ 的分量当假想插入重跑同一条判据 ⇒ 候选 52 条、两条通道都允许 52 条 ⇒ **补上母表示不改变"不保质子"**。质子那一问里唯一的**无条件**一步是"偶数条物质场 $\Rightarrow$ $3Q$ 为偶"（偶费米子数不是插入出来的）。报告 §10 的每个数字都由插值取自读数字典 `SEL`，无一处手抄。
8. **这条判据还被从引擎外面植错核一次**（`维护脚本/check_r11_mutation.py`，台账 `check_r11_mutation.json`）：上面几条门禁都是**引擎自己**跑的，这一条问的是"同余式写错了会不会有人报"。脚本把 `so10_reps.py` 连同依赖 `so10_chain.py` 复制进临时目录，在副本上各改错一处再重跑整台引擎（副本的产出只落在它自己的目录里，仓库那份报告逐字节未动），当前 6 例（变异体 4、良性 1）全 CAUGHT：基线 103 项全 PASS，且它打印的门禁数与磁盘上 `验证脚本/SO10表示论报告.json` 的 `tests` 条数**相等** —— 那条等值检查是这一族的"夹具塌陷"探测器，本项第一次跑就踩到它（副本少了 `so10_chain.py` ⇒ 引擎在前面几层就短路退出、只跑到一部分门禁，而"退出码非 0"这条弱判据会把那种塌陷当成捕获）。四个变异体各说一件事：只改三条路径里浮点那条（$2\pi$ 周期写成 $\pi$）⇒ 判决表"允许 8 条、禁戒 0 条"**一个字数都没动**，抓住它的只有"甲乙丙一致"那条断言 ⇒ "三台互不共享算术的机器"不是排场；改同余式（$N\mid 3Q$ 写成 $N\mid Q$）⇒ 同一句变"允许 7 条、禁戒 1 条"；把荷账的 $\sum_k$ 写成只取第一条场 ⇒ 四条 R11 门禁同时变红、判决表翻成"允许 0 条、禁戒 8 条"；把枚举的筛选条件换成上面第 1 条里已被否证的"3221 零权"projector ⇒ **禁戒数仍是 0**，动的只有含单态类数 8→7 ⇒ 第 6 条那句"候选非空"要有类数等式守着，光看"被禁几条"这一列，枚举塌陷是静默的。良性那例（只补一条尾注释、语义不动）不误报，否则这台仪器是"永远红"而不是"能红"。⇒ 本条加的是一道**防护**，不是新的物理读数，证据等级一律不动。

⇒ **L10 仍未关闭，本项不提升任何证据等级。**它关掉的是上一条待办（"算符选择定则未算"），换来的是三个更清楚的边界：(i) 那 8 条"允许"只过了**规范不变**这一关，Lorentz 指标、代（味）指标、Fierz 恒等式三关都没过 ⇒ 10 个不变张量结构 ≠ 10 个质子衰变算符，本项**不**回填 $\tau_p$，也不改 P4/P7 的判据与分级（08 §3 的 F7 因此没有"补救版"）；(ii) 判据只在 $U(1)_{B-L}$ 那一个因子导出的残留群上跑 ⇒ 若完整残留群是它的扩张（$SU(3)_c$ 与 $SU(2)_{L/R}$ 的中心、$Spin(10)$ 的整体形式），"允许"可能变严、"禁戒"不会变松 ⇒ **新登记待办**：完整残留群（含整体形式）；(iii) 只枚举到 $d=6$、只到物质场分量（$\pm$ 声明谱或假想插入的标量分量）⇒ 含导数算符、含共轭场的高维算符、$\nu^c$ 以外的轻子数破缺结构均未枚举。原缺口（位势、真空方向、两环、Yukawa 系数与味结构）一律保留。

---

## L11. Landau 极点

$U(1)_Y$ 的 $\beta$ 函数为正（$b_1=41/10>0$），耦合随能量上升，在 $\sim10^{41}$ GeV 处形式上发散（远超 $M_{\rm Pl}$，故在有效范围内不构成实践问题），但说明 $U(1)$ 扇区**不是紫外完备的**。

---

## L12. 禁闭与质量隙无解析证明

$SU(3)_C$ 的红外行为（禁闭、质量隙 $\Delta>0$）**无解析证明** —— 这是 Clay 数学研究所千禧年大奖问题之一。

格点 QCD 数值确立了禁闭与线性势 $V(r)\simeq\sigma r$（$\sigma\approx0.18\ \text{GeV}^2$），但**不是从主方程解析推出**。$m_p=938$ MeV 未被第一性原理算出。

---

## L13. 重子生成

Sakharov 三条件中的 CP 破坏：CKM 相给出的 CP 破坏量级比所需小约 $10^8$ 倍。电弱相变在 $m_h=125$ GeV 下为**交叉过渡**（非强一级相变），不满足非平衡条件。

**宇宙中物质多于反物质的原因，UFE-1 无法解释。**

---

## L14. 本文件的元局限

即使把 L1–L13 全部解决，仍有一个无法回避的问题：**我们不知道这套框架是否就是自然界的选择。** 现有数据支持它到极高精度，但"未被证伪"不等于"正确"。任何声称"统一场论已经完成"的表述，都不应由本项目背书。

---

## L15. 真正统一场论的路径（2026-09-19 增补）

> 本节把 L1–L13 的"不能做什么"转化为"应该怎么做"，依据是
> [算法联盟](../04_公共成果/算法联盟_全维自洽与归一化/README.md)
> 的不可能性定理链与 UFE-1 的验证结果。

### 不可能性定理链（已证）

| 定理 | 内容 | 封闭的路线 |
|---|---|---|
| **C** | 量纲代数不可能给出可检验的无量纲预言 | 所有"从 $c,\hbar,G$ 导出 $\alpha$"的尝试 |
| **H** | 初等几何作用量不能把 $\alpha$ 固定到观测值 | 螺旋/曲率作用量的变分极小化 |
| **I–VIII** | 代数派生恒有 $V\le0$；派生不产生信息 | 任何常数的代数乘幂组合 |

### 唯一未封闭的路线：动力学本征值

无量纲常数必须作为**本征值问题的解**出现，而非常数的代数组合。

$$\hat H|\psi\rangle = E|\psi\rangle \quad\Longrightarrow\quad \alpha = f(E_n, \text{拓扑量子数})$$

判定准则（定理 D）：$\text{nullity}_{\rm dyn}=n-\operatorname{rank}(J_E)=0$，即靶量在旋钮零空间上的微分 $dq|_{\ker J_E}=0$。

### UFE-1 → 真正统一场论的三阶段路径

#### 阶段一：巩固基线（UFE-1，当前状态）

UFE-1 作为 **ECSK + SM 的统一表述**已通过 47 项验证（PASS 39 / FAIL 2 / INFO 6）。
它是正确的**基线**，但不是统一场论——规范群是直积、26 参数全是输入。

**已完成**：反常消除、电弱精确检验、经典 GR 极限、中微子质量修复（L1）。
**定位**：UFT 达成度 2/6（与判据校准一致）。

#### 阶段二：四力真正统一（攻 UFT-2）

当前 $G=SO(1,3)\times SU(3)\times SU(2)\times U(1)$ 是**直积**，各因子独立。
真正统一需要把内部规范群嵌入单一李群（如 $SU(5)$、$SO(10)$、$E_6$），
或给出几何起源（如额外维紧致化的卡鲁扎-克莱因约化）。

**统一探针规格**（[统一探针报告](验证脚本/统一探针报告.md)）：
非 SUSY 统一需 $\Delta b$ 命中下表，且 $(M_{\rm int},M_{\rm GUT})$ 被钉死：

| $M_{\rm int}$ | $M_{\rm GUT}$ | 所需 $\Delta b$ | $\alpha_{\rm GUT}^{-1}$ |
|---|---|---|---|
| $10^3$ GeV | $2\times10^{16}$ GeV | $(5.47,0,-4.03)$ | 10.81 |
| $10^{10}$ GeV | $2\times10^{16}$ GeV | $(3.48,0,-4.25)$ | 29.45 |

**结构 no-go**：一整代矢量费米子对统一"盲"（$\Delta b\propto(1,1,1)$），
加任意代数都不统一——必须引入**非矢量型**新态。

**$\Delta b_3<0$ 不可达定理（2026-09-19 突破分析）**：
在所有物理标度下，非 SUSY 统一规格均要求 $\Delta b_3<0$。
但物质内容对 $SU(3)$ 的 $\beta$ 贡献只能为正：
- Weyl 费米子：$\Delta b_3 = +\frac{4}{3}T(R)$
- 复标量：$\Delta b_3 = +\frac{1}{3}T(R)$

因此**仅通过添加物质无法实现非 SUSY 的一环三耦合统一**。
若放松 $\Delta b_2=0$ 归一，设 $\Delta b_2=t$，解空间为
$\Delta b_1=t+5.47,\ \Delta b_3=t-4.03$；要 $\Delta b_3\ge0$ 需 $t\ge4.03$，
对应约 12 个 $SU(2)$ 二重态费米子——**极不自然**。

**两条可行路线**：
1. **SUSY 路线**：MSSM 的 $\Delta b=(2.5,\,4.17,\,4.0)$ 全为正，物质可达且自然。
2. **GUT 路线**：在 $M_{\rm GUT}$ 以上嵌入单一群（$SU(5)$、$SO(10)$、$E_6$），
   群论强制 $g_1=g_2=g_3=g_{\rm GUT}$，无需 $\Delta b$ 调整。

**SO(10) 嵌入的自然性（2026-09-20 突破）**：
UFE-1 每代费米子场内容与 $SO(10)$ 的 **16 维旋量表示**完全匹配：

| UFE-1 场 | $SU(3)\times SU(2)$ 表示 | $U(1)_Y$ | $SO(10)$ 16 分量 |
|---|---|---|---|
| $Q_L$ | $(3,2)$ | $+1/6$ | $(3,2)_{+1/6}$ |
| $u_R^c$ | $(\bar3,1)$ | $-2/3$ | $(\bar3,1)_{-2/3}$ |
| $d_R^c$ | $(\bar3,1)$ | $+1/3$ | $(\bar3,1)_{+1/3}$ |
| $L_L$ | $(1,2)$ | $-1/2$ | $(1,2)_{-1/2}$ |
| $e_R^c$ | $(1,1)$ | $+1$ | $(1,1)_{+1}$ |
| $\nu_R^c$ | $(1,1)$ | $0$ | $(1,1)_0$ |

每代超荷和 $\sum Y\cdot d_3\cdot d_2 = 0$（反常自动消除）。
**关键推论**：
- $\nu_R$ 的纳入不是随意的，而是 $SO(10)$ 对称性的**必然要求**（16 维表示的最后一个分量）。
- 电荷量子化 $Q=T_3+Y$ 在 $SO(10)$ 中由群论**自动给出**，无需额外假设。
- $SO(10)\supset SU(5)\times U(1)_{B-L}$，故 $SU(5)$ 是其子群；$B-L$ 作为 $U(1)$ 因子自然出现。
- **代价**：$SO(10)$ 伴随表示 45 中额外的规范玻色子 $(3,1)_{2/3}+(\bar3,1)_{-2/3}$ 需在中间标度获得质量，引入新的破缺参数。

**See-saw 机制与中微子质量（2026-09-20 突破）**：
$SO(10)$ 对称性破缺可给 $\nu_R$ 一个大 Majorana 质量 $M_R$，经 I 型 see-saw 给出轻中微子质量：

$$m_\nu \approx \frac{m_D^2}{M_R}, \qquad m_D = \frac{y_\nu v}{\sqrt2}$$

| $y_\nu$ 取值假设 | $m_D$ | 所需 $M_R$（使 $m_\nu\sim0.05$ eV） |
|---|---|---|
| $y_\nu \sim y_e \approx 3\times10^{-6}$ | 511 keV | $5\times10^{3}$ GeV |
| $y_\nu \sim y_\mu \approx 6\times10^{-4}$ | 106 MeV | $2\times10^{8}$ GeV |
| $y_\nu \sim y_\tau \approx 1\times10^{-2}$ | 1.78 GeV | $6\times10^{10}$ GeV |

**关键意义**：UFE-1 中 $y_\nu\sim3\times10^{-13}$（比 $y_e$ 小 7 个量级）极不自然；
$SO(10)$ see-saw 机制下 $y_\nu$ 可与带电轻子 Yukawa 同量级，中微子质量微小性由 $M_R$ 的大标度解释，
**无需极小耦合**。这为 L1（中微子 Yukawa 极小性）提供了自然解释路径。

**2026-09-23 反向检验（[SO(10) 链探针](验证脚本/SO10链统一报告.md) §5.5）**：上表把 $M_R$ 当**输入**、问"多大 $y_\nu$ 才够"。
`so10_chain.py` 反过来：$M_R$ 由三个实测规范耦合经一环统一**解出**（不掺任何中微子信息），
再按同一公式反推 $y_\nu$（取 $m_\nu=\sqrt{\Delta m^2_{atm}}=0.05$ eV、$v=246.22$ GeV）：

| 情形 | 解出的 $M_R$ (GeV) | 反推 $y_\nu$ |
|---|---|---|
| A（仅费米子） | $1.30\times10^{10}$ | $4.6\times10^{-3}$ |
| B（+ $\Phi(1,2,2)\oplus\Sigma(1,1,3)_0$） | $4.88\times10^{10}$ | $9.0\times10^{-3}$ |
| C（B + 双二重体不分裂） | $1.13\times10^{11}$ | $1.4\times10^{-2}$ |

⇒ 情形 A/B/C 的 $M_R$ **恰好**把 $y_\nu$ 抬到 $y_\tau\approx1.0\times10^{-2}$ 的量级 —— 两条独立路线
（规范统一 vs 中微子质量）落在同一标度上。
（**2026-09-24 数值修订**：本表原登记 C 为 $M_R=2.15\times10^{4}$ GeV、$y_\nu=6.0\times10^{-6}$ 并注"该情形
$M_{\rm GUT}>M_{\rm Pl}$，已否"。那两行数是台账 **F4** 所述 $b_2$ 重复计入缺陷的产物 —— 该缺陷同时把 C 压成
非物理解，故旧值与旧判据同源，一并作废；修复后 C 的 $M_R$ 由同一解给出，见报告 §5.5。
"三情形都落在 $y_\tau$ 量级"是**修复后**的陈述，不是修复前"只剩 A/B"的延续，且其跨度（$4.6\times10^{-3}$–$1.4\times10^{-2}$）
已接近 3 倍 ⇒ 这是量级一致，不是数值吻合。）
**但这只是量级一致，不是推导**：单一代主导、$O(1)$ 复系数与 flavor 结构全部忽略；
且本探针的标量谱**不含**能破 $B-L$ 的 $\Delta_R(1,1,3)_{B-L=2}\in 126_H$（已验证谱内三重态 $B-L=0$；
2026-09-23 更进一步：`so10_reps.py` 由 D5 权重格证明 $\dim\le210$ 内**只有** $126_H/\overline{126}_H$ 的中性分量能承担 $|\Delta(B-L)|=2$ 且保 $U(1)_{\rm em}$，见 [SO(10) 表示论报告](验证脚本/SO10表示论报告.md)），
故此处 $M_R$ 是 $3221\to LR$ 的**匹配标度**，不是被算出的 Majorana 质量。记为"不矛盾"，不记为预言。

#### 阶段二进展（2026-09-23）：SO(10) 破缺链已成为**可判定计算**

`so10_chain.py`（**55 项门禁**全 PASS，零依赖：引擎 S0–S5 + 解 S6 + 结论性散文同源 S7；2026-09-23 首版为 44 项，
$b_2$ 修复后 47 项，加 S7 系列后 55 项）把上面"路线 2（GUT 路线）"落到具体破缺链上。
要点（全表见 [SO10链统一报告](验证脚本/SO10链统一报告.md)）：

| 结论 | 内容 | 性质 |
|---|---|---|
| 归一化第一性 | $k_Y=5/3$、$k_{B-L}=8/3$、$\mathrm{Tr}_{16}Y^2=10/3$、$b=(41/10,-19/6,-7)$ 由 D5 权重格推导并回归 | 机制可信 |
| ~~否定性结果~~（**2026-09-24 撤销 = 台账 F4**） | ~~情形 C（双二重体不分裂）⇒ $M_{\rm GUT}=5.1\times10^{20}\ \mathrm{GeV}>M_{\rm Pl}$，该标量谱下不成立~~ → 该数由情形 C 的 $b_2$ **重复计入规范项**产生；修复后 $M_{\rm GUT}=2.293\times10^{16}<M_{\rm Pl}$ ⇒ 三条判据全过 | **不是否定结果**：曾被代码缺陷伪装成一条真实的排除 |
| 唯一可单点证伪的数 | 单阈 3221 链 $3\times3$ 定出 $M_{\rm GUT}=2.293\times10^{16}$–$1.990\times10^{17}$ GeV，$\alpha_{\rm GUT}^{-1}=45.42\text{–}47.82$（三情形并列区间，**不是**误差棒） | 依赖链选择与标量谱 |
| 证据上限 | 双阈链的"精确命中"是参数计数（3 未知/3 方程）；其次序窗口上沿与 3221 的 $M_{\rm GUT}$ **恒等**（同一极限）⇒ 三链只给**一个**数 | 不是三重确认 |
| 与 P4 的冲突 | 同一套 $X/Y$ 玻色子 ⇒ $\tau_p=3.4\times10^{35}$–$2.1\times10^{39}$ yr，而 P4 登记 $\tau_p=\infty$ | **互斥，未调和** |
| Higgs 分支规则（同日第二项） | `so10_reps.py`（该阶段 45 项自检全 PASS）：$\dim\le210$ 内**唯一**能承载 $\lvert\Delta(B-L)\rvert=2$ 且保 $U(1)_{\rm em}$ 的是 $126_H/\overline{126}_H$ 的 $\Delta_{L/R}$ 中性分量；$120_H$ 因 $(1,1,1)_{\pm2}$ 带 $\lvert Q\rvert=1$ 被否，$210_H$ 因 $\lvert B-L\rvert=2$ 全在 $(1,2,2)$ 被否 | 表示论层已定死；**位势/Yukawa 系数未算** |
| 分支规则系数（第三项，2026-09-24） | 同一 `so10_reps.py` 内的极大秩子群分支引擎（Klimyk–Springer；连同 R0–R5 共 51 项自检全 PASS）：11 个表示 × 3221/422 两条链**逐权重**给出分量清单，$\sum n_\lambda\dim_H=\dim\Lambda$ 逐项成立，142 个分量三路维数复核，$16_F$ 含量与标准模型对上 | 权重层内容已定死；当时跨链匹配尚未进门禁 |
| 跨链匹配（第四项，2026-09-24） | 同一引擎的子系统 Freudenthal + 精确有理线性代数（54 项自检全 PASS）：$\mathrm{Res}^{422}_{3221}\mathrm{Res}^{SO(10)}_{422}=\mathrm{Res}^{SO(10)}_{3221}$ 作为多重集在 11 个表示上逐项相等（R6.7）；$B-L$ 是 $\mathrm{span}(A_3)$ 内与 $A_2$ 正交的**唯一**方向（R6.8a），且在 $SU(4)$ 每个不可约表示上无迹（R6.8） | $422\to3221$ 那一步不再需要额外的 $U(1)$；仍是权重层陈述，**不含动力学** |
| 阈值层（第六项，2026-09-24） | `so10_thresholds.py`（55 项门禁全 PASS）：逐分量 $\partial\ln M_{\rm GUT}/\partial\ln M_j$ 与 36 个完整重解对账（T7.2 偏差 $\le10^{-9}$）；整块 $SO(10)$ 多重态放在共同标度时沿三条 SM 方向的 $\Delta b$ **严格相等**（$10_H\to1/3$、$45_H\to8/3$、$126_H\to35/3$，合计 $44/3$）⇒ $M_{\rm GUT}$、$M_R$ 在 $10^{-12}$ 内不动，只有 $\alpha_{\rm GUT}^{-1}$ 移动；同法给 R9 那条缺位定价（T8 族 15 项，报告 §9）：补入 $120_H$ 整块时 $c=28/3$ 的机制读数被定点（T8.9），代价是 $\alpha_{\rm GUT}^{-1}$ 由 $9.2031$ 变 $-15.3734$ ⇒ 出分支，且它给不出 $B-L$ 破缺（T8.5） | 一环口径的**响应系数**，不是标量谱的预言；两环、有限部分阈值仍未做；负号只排除"一环适用范围内它能补上缺口"，不排除自然界有该场 |
| d=4 Yukawa 通道（第八项，2026-09-24） | `so10_reps.py` 的 R7 层（该阶段 67 项自检全 PASS）：$\mathrm{Sym}^2(16)=10\oplus126$、$\wedge^2(16)=120$、$16\otimes\overline{16}=1\oplus45\oplus210$ ⇒ 手征对称通道 $10_H\oplus\overline{126}_H$、反对称 $120_H$、对消 $45_H\oplus210_H$，且手征 ∩ 对消 $=\varnothing$；$\nu_R^c$ 在 $16_F$ 内唯一，其 Majorana 所需权重 $-2\nu$ 在 $\dim\le210$ 内**只**由 $\overline{126}_H$ 携带、且正是该表示 $\Delta_R$ 的唯一中性成员 | 群论层已定死；**Yukawa 系数、味结构、位势与真空方向未算** ⇒ "允许"不等于"自然界有该场" |
| 不变张量簿记（第九项，2026-09-24） | 同一引擎的 R8 层（该阶段 75 项自检全 PASS）：**荷账本** $\dim V_0=m_V(0)$ 与**不变张量** $\mathrm{mult}((1),V)$ 分列记账，$(16\otimes16)、(16\otimes\overline{16})、(16^{\otimes4})$ 读数 $(0,0)、(16,1)、(960,2)$；不变张量基本定理 $\mathrm{mult}((1),A\otimes B)=\delta_{B,\bar A}$ 在 52 个表示对上现场核验（0 反例），四条不共享代码的路径对 $16\otimes16\otimes\overline{16}\otimes\overline{16}$ 同为 3，两种结合方式的权重多重集逐权相等 ⇒ §6 通道表由第二条路按标号独立复算通过 | 群论层已定死；数的是**不变张量个数 ≠ 算符个数**（Lorentz／味／Fierz 三关未过），不回填 $\tau_p$；物质-only 算符自动 $\Delta(B-L)=0$ ⇒ $\lvert \Delta(B-L)\rvert =2$ 必插 $\overline{126}_H$ |
| 不变张量的道分解（第十项，2026-09-24） | 同一引擎的 R9 层（该阶段 81 项自检全 PASS）：把 R8 的配对和 $\mathrm{mult}((1),A\otimes A)=\sum_Rn_Rn_{\bar R}$ **按道拆开** ⇒ $16\otimes16$ 的 $10\to1$、$120\to1$、$126\to0$（合计 2 = R8 那个数）、$16\otimes\overline{16}$ 的 $(1)$、$45$、$210$ 各 1（合计 3）；$126$ 是六条道里唯一"在场却不闭合"的，而它正是 $\overline{126}_H$（唯一承载 $\nu_R$ Majorana 质量者）的伙伴；两条闭合道分处 $\mathrm{Sym}^2$ 与 $\wedge^2$ 两个槽；单个 $126/\overline{126}$ **插不进** $16^{\otimes4}$（三种结合 + 5 个可直解道对全为 0），成对插入给 $12,21,20$ 且共轭镜像**交叉**相等；配对和在本层第一次改走**标号**路径（同一张表按标号 12、按名字 3，因 13 条标号在登记表外） | 群论层已定死；仍是**张量层**陈述、不回填 $\tau_p$；"道 $\to$ 母表示"是情形文字给出的**输入**（$\Phi(1,2,2)$ 的 4 条权重同时落在三条道里 ⇒ 包含判据在同侧退化），按该输入两条闭合手征道里只有 $10_H$ 有母表示、$120_H$ 无人声明；"按名字查共轭"只在 $\dim\le210$ 内可用 |

| 残留离散规范对称（第十一项，2026-09-24） | 同一引擎的 R10 层（该阶段 92 项自检全 PASS）：$B-L$ 荷格由 $16_F$ 定死（$g_0=1/3$，加满 $\dim\le210$ 的 11 个表示不变）⇒ 圆周 $\alpha_0=2\pi/g_0=6\pi$（浮点枚举独立扫得同一个数）、残留阶 $N=q_\Delta/g_0$；读数 $\Delta(B-L)=1\to\mathbb{Z}_3$（奇数阶 ⇒ 无 2 阶元，$(-1)^{3(B-L)}$ 在该 v.e.v. 上取 $-1$ ⇒ **物质字称被这一步破掉**）、$\Delta(B-L)=2\to\mathbb{Z}_6$（其 2 阶元在 $\alpha=3\pi$，才是物质字称；它在其余 8 个表示的每条权重上取 $+1$，例外 $16,\overline{16},144$ 由数据给出 ⇒ "标量一定偶"是**算出来**的，不是设定的字称；3 阶元实测与 $A_2$ 标号满足 $3(B-L)\equiv s(p-q)\pmod3$）；每个候选的 $N$ 被 3 整除 ⇒ triality 躲不掉；两种植入的归一化滑倒（误取 $2\pi$ 周期、只缩 $q_\Delta$）分别复现 $\mathbb{Z}_2$ 与"无解" | **推翻了旧散文**"残留 $Z_2$（物质字称，对质子稳定反而有利）"⇒ 08 §3 的 F7，报告/本文六处已按读数改写；只算 $U(1)_{B-L}$ 一个因子，422 链仍未做；**算符选择定则已于同日第十二项判出**（见下一行）⇒ 不回填 $\tau_p$；且两个残留群都是**条件读数**（承载者不在三个情形字面声明的谱里，R10.10） |
| 低能算符选择定则（第十二项，2026-09-24） | 同一引擎的 R11 层（该阶段 103 项自检全 PASS）：判据压成一条同余式（$N\mid 3Q$，$Q=\sum_k(B-L)_k$），三条互不共享算术的实现（整数整除／群元代回精确有理／浮点角度）必须同判决，且 $\mathbb{Z}_3$（$3Q\bmod 3$）与 $\mathbb{Z}_2$（$3Q\bmod 2$）取与须等于甲；低能计数全部放在 $Y$ 切片上（$SU(2)_L$ 单根 $\perp Y$、$SU(2)_R$ 单根与 $Y$ 内积 $=144$ ⇒ "3221 零权"不是 projector：$16_F$ 零权 0 个却有 1 个单态 $\nu^c$，且两个方向都有反例 ⇒ 零权数与单态数无不等式关系）；$\mathbb{Z}_3$ 的荷完全由色携带（166 条场逐条核 $3(B-L)\equiv s(p-q)\pmod 3$，四种"荷号 × 符号"配对全测，教材号 $s=+$／引擎号 $s=-$，差的正是 R5.7/R6.6 那条登记过的约定差）；读数：物质双线性里唯一低能不变量是 $\nu^c\nu^c$（$\Delta L=-2$、$3Q=6$，两通道都允许），d=6 的 126 个场内容类含 SM 单态 8 条（10 个不变张量结构；三条计数路径同为 117,117,117）**全部允许、禁 0 条**，其中 $\Delta B\ne0$ 且 $\Delta L\ne0$ 的 3 条 $\Delta(B-L)$ 全为 0 ⇒ **残留离散规范对称性不保质子**；"禁 0 条"与"枚举到 0 条"由四处正对照区分（声明谱 d=4 候选 17 条；插入奇 $3(B-L)$ 标量 ⇒ 40 条候选被 $\mathbb{Z}_6$ **判禁 40 条**；$\mathbb{Z}_6$ 误读成 $\mathbb{Z}_3$ ⇒ 40 条判决差异；整套荷换号 ⇒ 翻转 0 条）；**判据本身另有一道外部变异核验**（`维护脚本/check_r11_mutation.py`，台账 `check_r11_mutation.json`：6 例（变异体 4、良性 1）全 CAUGHT、基线 103 项全 PASS 且门禁数与磁盘报告 JSON 的 `tests` 条数相等 —— 只改三条路径里浮点那条的周期 ⇒ 判决表一字不动、抓住它的只有"甲乙丙一致"；改同余式 ⇒ 允许 7 条、禁戒 1 条；荷账丢求和号 ⇒ 允许 0 条、禁戒 8 条；枚举改用那个已否证的错 projector ⇒ 禁戒数仍是 0 而含单态类数 8→7） | **推翻的是一条读法而非物理**：它否掉"用残留群救质子"这条论证（F7 之后无补救版），但**不**给出质子会衰变的速率；那 8 条"允许"只过了规范不变这一关（Lorentz／味／Fierz 三关未过，10 个不变张量结构 ≠ 10 个算符）⇒ 不回填 $\tau_p$、P4/P7 判据与分级一律不动；整层继承 R10.10 的条件性（声明谱里只有 $10_H\oplus45_H$，两个交集皆空；假想补入 $126/\overline{126}$ 重跑 ⇒ 候选 52 条、两通道全允许 ⇒ 判决不变）；只枚举到 $d=6$、只跑在 $U(1)_{B-L}$ 那一个因子导出的残留群上 ⇒ 完整残留群（含 $SU(3)_c$／$SU(2)_{L/R}$ 中心与 $Spin(10)$ 整体形式）登记为**新待办** |

⇒ 阶段二**未**达成"四力真正统一"，但已从"路线候选"推进为"带数字、带否证条件、且与本项目自身
预言冲突"的具体计算。剩余缺口按权重排列：① ~~Higgs 扇区分支规则的第一性推导~~ **已于 2026-09-23/24 完成**
（表示论层见 [SO(10) 表示论报告](验证脚本/SO10表示论报告.md) §4；**分支规则系数**见其 §5，
$126_H$ 的必要性由 Cartan 恒等式推出，情形 B 不再可称为"$B-L$ 破缺"）
——**未**完成的部分收窄为：Yukawa 系数（哪个拷贝耦合哪一代）、$126_H$ 位势与真空方向
（同日第八项再收窄一次：**"$d=4$ 可重整算符允许哪些 Higgs"本身也成了推导**，故剩下的只有系数与味结构，不再是"能否耦合"；
同日第九项把同一批结论用**第二条不共享代码的算法**（直接数不变张量）复算了一遍，没有新增物理内容，但通道表不再只有一条路的读数；
同日第十项把那两个不变张量落到**道**的名字上（$10$ 与 $120$ 各一个），于是缺口换了形状：不再是"能不能凑出不变张量"，
而是"这条道的母表示放进了谱没有"——而后者由情形文字给出（两条闭合手征道里只有 $10_H$ 被声明）⇒ 仍是标量谱输入，不是新的收窄）；
$422\to3221$ 跨链匹配门禁**已于同日第四项完成**（R6.7/R6.8a/R6.8，其时 54 项自检全 PASS；现 103 项，多的 49 项 = 第七项的跨仪器对账 4 项 + 第八项的 d=4 Yukawa 通道层 9 项 + 第九项的不变张量簿记层 8 项 + 第十项的不变张量道分解层 6 项 + 第十一项的残留离散规范对称层 11 项 + 第十二项的低能算符选择定则层 11 项）；
② ~~阈值修正 + 二环~~ **阈值已于 2026-09-24 在一环执行**（第五、六项进展），**二环仍缺**（两环系数矩阵无法在仓库内核验 ⇒ 不凭记忆写）——
原本以为这才是压掉 $M_{\rm GUT}$ 区间摆动的主项，而阈值层的实测结论是：**整块共同标度的移动不动 $M_{\rm GUT}$，只有块内分裂动它**；
③ P4 与质子衰变的取舍（需要先解决 L10 才能谈。**2026-09-24 第十一项更正**：此处原先"新添"的那条
相关事实——"$16_H$ 单步破 $B-L$ 会残留 $Z_2$ 物质字称"——是从未算过的散文，读数是
$\Delta(B-L)=1$ 那一步留 $\mathbb{Z}_3$ 且**把物质字称破掉**、$\Delta(B-L)=2$ 那一步才留
$\mathbb{Z}_6$（其 2 阶元那半边才是物质字称）⇒ 这条取舍**少了一个它以为自己有的对手**：走 $16_H$
通道时，并没有一个残留 $\mathbb{Z}_2$ 在替质子稳定说话（08 §3 的 F7）。残留群到底禁不禁
$u^cd^cd^c$、$QQQL$ 一类算符？**这一问已于同日第十二项判出**（报告 §10：d=6 的 126 个场内容类里含 SM 单态 8 条，在 $\mathbb{Z}_3$ 与 $\mathbb{Z}_6$ 两条通道下**全部允许**、禁 0 条，其中 $\Delta B\ne0$ 且 $\Delta L\ne0$ 的 3 条 $\Delta(B-L)$ 全为 0 也全部允许）⇒ **残留群不保质子**，这条取舍的对手不是"少了一个"而是"从来不是对手"；那 8 条只过了规范不变这一关（Lorentz／味／Fierz 未过）⇒ 此处仍不回填 $\tau_p$）。

#### 阶段三：动力学本征值派生（攻 UFT-3）

这是最硬的缺口。需要构造一个本征值问题，使至少一个无量纲常数
（$\alpha$、$m_\mu/m_e$、$\sin^2\theta_W$ 等）作为解自然出现。

**定理 F/G 的规格**：算子谱路线可行但需近消去——
$\alpha^2=\text{近消去残余}=\text{近似简并的破缺参数}$。
所需调参是 $O(\alpha^2)$（小量），与几何作用量的 $O(\alpha^{-2})$（大量）定性不同。

**可证伪阈值**（派生核算 VIII）：把账本记到 17 位十进制，
二分律可能被击穿——这是**可机器验证**的定量预言。

### 诚实边界

- 以上是**路径分析**，不是"统一场论已完成"。
- 阶段二、三均未实现；当前仍停在阶段一。
- UFT-6（外部验证）不在本项目权限内。

---

## 局限清单汇总

| 编号 | 局限 | 严重度 | 是否已解决 |
|---|---|---|---|
| L1 | 中微子质量为 0 | ~~致命（已证伪）~~ → 已修复 | ✅（代价：+7 参数、$y_\nu$ 极小性未解；SO(10) see-saw 提供自然解释路径） |
| L2 | $\alpha$ 无法推导 | 严重 | ❌（已加固：定理 C/E 封死量纲代数、几何作用量；定理 F/G 判算子谱路线「条件可行」） |
| L3 | 26 个输入参数 | 严重 | ❌ |
| L4 | 层级问题 | 严重 | ❌ |
| L5 | 代数 3 无解释 | 中等 | ❌ |
| L6 | 强 CP | 中等 | ❌ |
| L7 | 宇宙学常数 | **致命量级差距** | ❌ |
| L8 | 暗物质缺失 | 严重 | ❌ |
| L9 | 引力未量子化 | 根本 | ❌ |
| L10 | 耦合不统一 | 严重 | ❌（2026-09-23：SO(10) 三链一环已可判定，单阈链定出 $M_{\rm GUT}=2.293\times10^{16}$–$1.990\times10^{17}$ GeV、$\alpha_{\rm GUT}^{-1}=45.42\text{–}47.82$（三情形并列区间），但与 P4 互斥；2026-09-23/24：Higgs 分支规则的**表示论层**、**分支规则系数**与**跨链匹配**均由 D5 权重格推导（$\lvert\Delta(B-L)\rvert=2$ 只能走 $126_H/\overline{126}_H$；11 表示 × 两链逐权重表 + $422\to3221$ 路径无关）；同日阈值层（逐分量 $\Delta b$ + 统一点响应 + 补入 $126_H$ 重跑）已在一环执行；同日 d=4 Yukawa 通道层再把"$16_F16_FH$ 允许哪些 Higgs"变成推导（对称通道 $10_H\oplus\overline{126}_H$、反对称 $120_H$、对消 $45_H\oplus210_H$；see-saw 的两条独立判据落在同一个表示 $\overline{126}_H$ 上）；同日不变张量簿记层用**第二条不共享代码的算法**（直接数 $\mathrm{mult}((1),V)$，与荷账本 $\dim V_0$ 分列）复算了同一张通道表，并立起"手征物质无裸质量项""物质-only 不变算符自动 $\Delta(B-L)=0$、要 $\lvert \Delta(B-L)\rvert =2$ 必插 $\overline{126}_H$"两条读数；同日道分解层（R9）把 R8 那条配对和**按道拆开**，读出 $10$、$120$ 各承载一个不变张量而 $126$ "在场却不闭合"、单个 $126/\overline{126}$ **插不进** $16^{\otimes4}$ 而成对插入才非零，并把该配对和第一次改走**标号**路径（登记范围外"按名字查共轭"会把真实伙伴静默算成 0）⇒ 两条闭合手征道里只有 $10_H$ 在谱里有母表示，而这是情形文字给出的**输入**、不是算出的结论）；同日阈值层又给这条缺位补上价签（T8 族 15 项，报告 §9）：把 $120_H$ 送进同一台分量机器重跑，整块补入按 T6 机制不动 $M_{\rm GUT}$（$c(120_H)=28/3$ 的移动量被整块求和定点）却把 $\alpha_{\rm GUT}^{-1}$ 由 $9.2031$ 吃到 $-15.3734$ ⇒ 一环解出分支，分裂补法动 $\log_{10}M_{\rm GUT}$ 达 $-0.8583$ 且同样出分支，而它的 $(1,1,1)_{\pm2}$ 带 $\lvert Q\rvert=1$ ⇒ **这条道给不出 $B-L$ 破缺** ⇒ 缺口仍在，但不再是"没人算过"；同日残留离散规范对称层（R10 族 11 项）把"$B-L$ 破缺之后**还剩哪个离散规范对称性**"也变成格上的读数——荷格 $g_0=1/3$ 由 $16_F$ 一家定死 ⇒ 圆周 $\alpha_0=2\pi/g_0=6\pi$、残留阶 $N=q_\Delta/g_0$：$\Delta(B-L)=1$ 留**奇数阶** $\mathbb{Z}_3$（物质字称候选元 $(-1)^{3(B-L)}$ 在该 v.e.v. 上取 $-1$ ⇒ 它**被这一步破掉**），$\Delta(B-L)=2$ 才有那个 2 阶元（$\mathbb{Z}_6=\mathbb{Z}_2\times\mathbb{Z}_3$）⇒ 旧散文"单步破缺残留 $\mathbb{Z}_2$（物质字称，对质子稳定反而有利）"从未被算过、由读数推翻并登记为台账 **F7**；同日低能算符选择定则层（R11 族 11 项，报告 §10）接着答完 F7 留下的那一问：判据是一条同余式 $N\mid 3Q$（$Q=\sum_k(B-L)_k$；整数整除／群元代回精确有理／浮点角度三条不共享算术必须同判决），低能计数放在 $Y$ 切片上（$SU(2)_R$ 单根与 $Y$ 内积 $=144$ ⇒ "3221 零权"不是 projector，两个方向都有反例 ⇒ 零权数与单态数无不等式关系），读数是 d=6 的 126 个场内容类里含 SM 单态 8 条（10 个不变张量结构）**全部允许、禁 0 条**，其中 $\Delta B\ne0$ 且 $\Delta L\ne0$ 的 3 条 $\Delta(B-L)$ 全为 0，物质双线性里唯一的低能不变量是 $\nu^c\nu^c$ ⇒ **残留离散规范对称性不保质子**（这只否定"用残留群救质子"那条论证，**不**给衰变速率）；L10 的缺口本身**不变**（两个残留群与其算符判决同为条件读数：承载者 $126_H/\overline{126}_H$、$16_H/\overline{16}_H$ 都不在链探针声明的谱里——假想补入 $126/\overline{126}$ 重跑同一条判据给 52 条候选、两通道全允许，判决不动；且本层只跑在 $U(1)_{B-L}$ 那一个因子导出的残留群上，含 $SU(3)_c$／$SU(2)_{L/R}$ 中心与 $Spin(10)$ 整体形式的**完整残留群**登记为新待办）。三台仪器合计 **213 项门禁全 PASS**（103 + 55 + 55）。同日的 $b_2$ 缺陷修复撤销了此前的"情形 C 排除"（台账 F4）⇒ 区间两端倍数由 3.8（$A/B$）变为 **8.7**（$A/C$），诚实边界因此**变宽而不是变窄**；该倍数现已由 `so10_reps.py` 的 R2.sw0–R2.sw3 与链探针当场对账，不再写死（第七项）。未闭合的部分：二环（两环系数矩阵无法在仓库内核验 ⇒ 未实现）+ 有限部分阈值 + $126_H$ 位势 + Yukawa 系数与味结构） |
| L11 | Landau 极点 | 轻微（超 $M_{\rm Pl}$） | ❌ |
| L12 | 禁闭无解析证明 | 根本（数学） | ❌ |
| L13 | 重子生成 | 严重 | ❌ |
| L14 | 理论选择本身 | 元问题 | — |
| L15 | 真正统一场论的路径 | 方向性 | ❌（阶段二、三未实现；路径已明确） |

> **给贡献者**：解决上述任何一项，都是比"再写一份统一场论综述"更有价值的贡献。优先推荐 L1（最小扩展 $\nu_R$，可立即计算）与 L10（加入超对称或中间尺度新物理，可立即重算 [验证报告](验证脚本/验证报告.md) 的 R1）。**长远路线**：L15 的阶段二（四力真正统一，攻 UFT-2）与阶段三（动力学本征值派生，攻 UFT-3）是从基线走向真正统一场论的唯一路径。

[返回理论核心](../README.md)
